[package]
name = "cairn-git"
version = "0.1.0"
edition = "2021"

[lib]
name = "cairn_git"
path = "src/lib.rs"

[dependencies]
git2 = { version = "0.18", default-features = false, features = ["vendored-libgit2"] }

[dev-dependencies]
tempfile = "3"
