# Cairn SSH/SFTP v1 — Implementation Plan → `phase-2-ssh-v1` / `v0.2.0-alpha.1`

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Cairn이 `~/.ssh/config` 기반으로 임의 호스트에 SFTP 연결 → 기존 Tab 모델을 확장한 단일 페인 원격 탐색 + 기본 파일 조작(rename/delete/mkdir) + drag-drop 양방향 전송을 제공. Cloudflare Tunnel / AWS SSM ProxyCommand 호스트 1급 시민.

**Architecture:** 신규 `cairn-ssh` Rust crate (russh 0.44 + russh-sftp 2.0 + russh-keys) + Swift `FileSystemProvider` 프로토콜 추상화. Config 해석은 `ssh -G` subprocess 위임. 연결은 `(user, host, port, config-hash)` pool로 dedup + 5분 idle timeout.

**Tech Stack:** Swift 5.9 · SwiftUI · AppKit · macOS 14 | Rust 1.85 · swift-bridge 0.1.59 · **russh 0.44 · russh-sftp 2.0 · russh-keys 0.44 · sha2 0.10 · parking_lot (new)** · tokio (existing) · OpenSSH client (macOS 기본 `/usr/bin/ssh`)

**Working directory:** `/Users/cyj/workspace/personal/cairn` (main, HEAD ≈ `bbf9505` + spec `3d1aa9d`)

**Parent spec:** `docs/superpowers/specs/2026-04-23-cairn-ssh-v1-design.md`

**Predecessor milestone:** M1.8 (완료)

**Deliverable verification:**
- `cargo fmt --check` / `cargo clippy -D warnings` / `cargo test --workspace` green (cairn-ssh 포함)
- `xcodebuild test` PASS — 기존 60+ tests + 신규 ~15
- `make build && make run` 후 수동 DoD (Task 26) 13 항목 전부 PASS
- `git tag phase-2-ssh-v1` + `git tag v0.2.0-alpha.1` (같은 HEAD)

**특이사항:**
- **Pre-check**: 본 브랜치에 SSH 착수 전 남아있는 uncommitted 변경(Enter/Cmd+Down/마우스 back 핫픽스)을 먼저 commit 하거나 `git stash` 해야 함. Task 0 참조.
- **FFI 변경 대폭** — swift-bridge Generated diff 정상.
- **Rust dep 신규** — 첫 빌드 5-10분 추가.
- **Integration tests**(`CAIRN_SSH_IT=1`)는 docker openssh-server 필요. CI에선 skip, 로컬/release 전만 실행.
- **커밋 메시지 verbatim.** push는 수동. AI 어트리뷰션 trailer 금지 (프로젝트 preference).

---

## Task 개요 (총 26 개)

### Phase 0 — Pre-check (Task 0)
0. 작업 트리 정리 (uncommitted 핫픽스 commit) + 브랜치 베이스 확인

### Phase A — Rust `cairn-ssh` crate (Tasks 1-7)
1. Crate 스켈레톤 + 타입 (ConnKey, SshError, ResolvedConfig)
2. `config.rs` — `ssh -G` 해석 + shallow host list (Include walker)
3. `hostkey.rs` + `known_hosts_hash.rs` — fingerprint + match + append
4. `proxy.rs` — ChildIoStream + ProxyCommand subprocess
5. `pool.rs` + `auth.rs` — connect / dedup / idle reap / auth 시퀀스
6. `sftp.rs` — list/stat/mkdir/rename/unlink + copy-data 감지
7. `transfer.rs` — upload/download with progress + cancel flag

### Phase B — FFI (Task 8)
8. `cairn-ffi/src/ssh.rs` + swift-bridge 바인딩 regen + build 검증

### Phase C — Swift provider 레이어 (Tasks 9-11)
9. `FileSystemProvider` protocol + `FSPath` + `LocalFileSystemProvider` (pass-through)
10. `FolderModel` + `Tab` FSPath 마이그레이션 (로컬 회귀 zero)
11. `SshFileSystemProvider` 구현

### Phase D — Swift 런타임 서비스 (Tasks 12-14)
12. `SshPoolService` + `HostKeyAlertResolver` + `PassphraseResolver` + Keychain
13. `HostMetadataStore` sidecar + `ConfigFileWatcher` (FSEvents)
14. `SshConfigWriter` (append-only) + `SshConfigService` (host list 읽기)

### Phase E — Sidebar + Connect (Tasks 15-17)
15. 사이드바 "Remote Hosts" 섹션 + `RemoteHostRow`
16. Connect sheet (model + view, ⇧⌘K)
17. TOFU / Changed-key / Passphrase alert views

### Phase F — Tab chrome + 원격 파일 조작 (Tasks 18-19)
18. Tab chip SSH 배지 + `ssh://` breadcrumb + window title
19. FileListCoordinator provider-dispatched rename/delete/mkdir + 원격 delete 확인 모달

### Phase G — Transfer (Tasks 20-22)
20. `TransferController` + `TransferJob` 타입 + 호스트당 serial 실행
21. `TransferHudChip` + `TransferPopoverView` + pulse 애니메이션
22. 크로스-provider drag-drop in FileListNSTableView

### Phase H — Preview + 캐시 (Task 23)
23. 텍스트 head streaming preview + `RemoteCacheStore` + Quick Look + Open With + "Clear Remote Cache"

### Phase I — 에러 인라인 + 마무리 (Tasks 24-26)
24. 인라인 에러 상태 (connecting / auth-failed / keepalive-lost / listing-failed)
25. Integration tests + ssh_config FSEvents → 사이드바 자동 reload
26. 로컬 CI + 수동 DoD + `git tag phase-2-ssh-v1` + `v0.2.0-alpha.1`

---

## Task 0: 작업 트리 정리

**Files:** 없음 (git 작업만)

- [ ] **Step 1: 현재 상태 확인**

```bash
git status
```

Expected: Enter/Cmd+Down/마우스 back 관련 변경이 `apps/Sources/CairnApp.swift`, `apps/Sources/ContentView.swift`, `apps/Sources/Views/FileList/FileListCoordinator.swift`, `FileListNSTableView.swift`, `FileListView.swift`에 있음.

- [ ] **Step 2: 핫픽스 commit (스코프 분리)**

```bash
git add apps/Sources/CairnApp.swift apps/Sources/ContentView.swift apps/Sources/Views/FileList/FileListCoordinator.swift apps/Sources/Views/FileList/FileListNSTableView.swift apps/Sources/Views/FileList/FileListView.swift
git commit -m "fix(ux): Enter→rename, Cmd+Down→open-selected, mouse side→history nav

- Enter on selected row starts Finder-style inline rename via NSTextField
  editable mode flip, committed via FileManager.moveItem with undo.
- Cmd+Down opens the selected item (folder=navigate, file=NSWorkspace.open),
  wired as 'Open Selected' in Navigate menu.
- Mouse button 3/4 (side back/forward) routes to tab.goBack()/goForward()
  via NSEvent.addLocalMonitorForEvents scoped to ContentView lifecycle."
```

- [ ] **Step 3: 깨끗한 상태 확인**

```bash
git status
```

Expected: `working tree clean`. 이 HEAD에서 SSH 작업 시작.

---

## Task 1: `cairn-ssh` crate 스켈레톤 + 타입

**Files:**
- Create: `crates/cairn-ssh/Cargo.toml`
- Create: `crates/cairn-ssh/src/lib.rs`
- Create: `crates/cairn-ssh/src/error.rs`
- Create: `crates/cairn-ssh/src/types.rs`
- Modify: `Cargo.toml` (workspace members + workspace deps)

- [ ] **Step 1: workspace에 crate 등록**

Repo root `Cargo.toml`의 `[workspace].members` 배열 마지막에 `"crates/cairn-ssh"` 추가.

`[workspace.dependencies]`에 추가:

```toml
russh       = "0.44"
russh-sftp  = "2.0"
russh-keys  = "0.44"
sha2        = "0.10"
parking_lot = "0.12"
tracing     = "0.1"
tokio       = { version = "1", features = ["full"] }
```

- [ ] **Step 2: `crates/cairn-ssh/Cargo.toml` 작성**

```toml
[package]
name = "cairn-ssh"
version.workspace = true
edition.workspace = true

[lib]
name = "cairn_ssh"
path = "src/lib.rs"

[dependencies]
russh       = { workspace = true }
russh-sftp  = { workspace = true }
russh-keys  = { workspace = true }
tokio       = { workspace = true }
sha2        = { workspace = true }
parking_lot = { workspace = true }
tracing     = { workspace = true }
thiserror   = { workspace = true }
anyhow      = { workspace = true }

[dev-dependencies]
tokio = { workspace = true, features = ["test-util", "macros"] }
tempfile = "3"
```

- [ ] **Step 3: `src/error.rs` — `SshError` enum**

```rust
use std::io;
use std::path::PathBuf;
use thiserror::Error;

#[derive(Debug, Error)]
pub enum SshError {
    #[error("Couldn't resolve ssh_config for {host}: {msg}")]
    ConfigResolution { host: String, msg: String },

    #[error("Proxy command failed (exit {exit_code}): {stderr_preview}")]
    ProxyCommandFailed { exit_code: i32, stderr_preview: String },

    #[error("Couldn't spawn proxy command ({cmd}): {source}")]
    ProxyCommandSpawn {
        cmd: String,
        #[source]
        source: io::Error,
    },

    #[error("ProxyJump without ProxyCommand is not supported in v1")]
    ProxyJumpNotSupported,

    #[error("Network unreachable: {host}:{port}")]
    NetworkUnreachable { host: String, port: u16 },

    #[error("Host key mismatch for {host} — possible MITM")]
    HostKeyMismatch { host: String },

    #[error("Host key not accepted")]
    HostKeyRejected,

    #[error("No authentication method succeeded (tried: {tried})")]
    AuthNoMethods { tried: String },

    #[error("Server requires {kind} authentication — not supported in v1")]
    AuthKindNotSupported { kind: &'static str },

    #[error("Couldn't unlock key file {path}")]
    KeyPassphraseFailed { path: PathBuf },

    #[error("SFTP: {0}")]
    SftpProtocol(String),

    #[error("Permission denied: {0}")]
    SftpPermissionDenied(String),

    #[error("Not found: {0}")]
    SftpNotFound(String),

    #[error("No space left on remote")]
    SftpNoSpace,

    #[error("Connection to {host} lost")]
    ConnectionLost { host: String },

    #[error("Cancelled")]
    Cancelled,

    #[error(transparent)]
    Io(#[from] io::Error),

    #[error("Russh: {0}")]
    Russh(String),
}

pub type Result<T> = std::result::Result<T, SshError>;
```

- [ ] **Step 4: `src/types.rs` — `ConnKey`, `ResolvedConfig`, `ConnectSpec`**

```rust
use std::path::PathBuf;
use std::time::Duration;

#[derive(Debug, Clone, Copy, PartialEq, Eq)]
pub enum StrictMode {
    Yes,
    AcceptNew,
    Ask,
    No,
}

#[derive(Debug, Clone)]
pub struct ResolvedConfig {
    pub hostname: String,
    pub port: u16,
    pub user: String,
    pub identity_files: Vec<PathBuf>,
    pub identity_agent: Option<PathBuf>,
    pub proxy_command: Option<String>,
    pub proxy_jump: Option<String>,
    pub server_alive_interval: Duration,
    pub server_alive_count_max: u32,
    pub strict_host_key_checking: StrictMode,
    pub user_known_hosts_file: Vec<PathBuf>,
    pub global_known_hosts_file: Vec<PathBuf>,
    pub host_key_algorithms: Vec<String>,
    pub preferred_authentications: Vec<String>,
    pub compression: bool,
    pub hash_known_hosts: bool,
}

#[derive(Debug, Clone, Hash, PartialEq, Eq)]
pub struct ConnKey {
    pub user: String,
    pub hostname: String,
    pub port: u16,
    pub config_hash: [u8; 16],
}

impl ConnKey {
    pub fn from_resolved(resolved: &ResolvedConfig) -> Self {
        use sha2::{Digest, Sha256};
        let mut h = Sha256::new();
        if let Some(pc) = &resolved.proxy_command { h.update(pc.as_bytes()); }
        for id in &resolved.identity_files { h.update(id.to_string_lossy().as_bytes()); }
        for algo in &resolved.host_key_algorithms { h.update(algo.as_bytes()); }
        if let Some(ia) = &resolved.identity_agent { h.update(ia.to_string_lossy().as_bytes()); }
        let full = h.finalize();
        let mut out = [0u8; 16];
        out.copy_from_slice(&full[..16]);
        Self {
            user: resolved.user.clone(),
            hostname: resolved.hostname.clone(),
            port: resolved.port,
            config_hash: out,
        }
    }
}

/// One-off connection spec — user may override user/host/port/path/proxy on
/// top of the ssh_config-resolved defaults. All overrides are optional.
#[derive(Debug, Clone)]
pub struct ConnectSpec {
    pub host_alias: String,       // ssh_config 이름 or bare hostname
    pub user_override: Option<String>,
    pub port_override: Option<u16>,
    pub identity_file_override: Option<PathBuf>,
    pub proxy_command_override: Option<String>,
}
```

- [ ] **Step 5: `src/lib.rs` — 모듈 선언**

```rust
//! SSH/SFTP backend for Cairn. Config resolution is delegated to the system
//! `ssh -G` binary so ProxyCommand / ProxyJump / Match / Include work without
//! a Rust-side ssh_config parser.

pub mod error;
pub mod types;

pub use error::{Result, SshError};
pub use types::{ConnKey, ConnectSpec, ResolvedConfig, StrictMode};
```

- [ ] **Step 6: 빌드 검증**

```bash
cargo build -p cairn-ssh
```

Expected: PASS (dependency fetch + compile, 5-10분 첫 실행).

- [ ] **Step 7: Commit**

```bash
git add Cargo.toml crates/cairn-ssh/
git commit -m "feat(ssh): scaffold cairn-ssh crate with error + base types

Adds the crate skeleton with SshError enum (16 variants covering
config/proxy/network/auth/sftp/io failure modes) and core types:
ResolvedConfig mirrors 'ssh -G' output; ConnKey is the pool
dedup key hashed from proxy_command + identity_files + host_key_algorithms
+ identity_agent; ConnectSpec captures one-off overrides.

Deps: russh 0.44, russh-sftp 2.0, russh-keys 0.44, sha2 0.10,
parking_lot, tracing. Shared via workspace."
```

---

## Task 2: `config.rs` — `ssh -G` 해석 + shallow host list

**Files:**
- Create: `crates/cairn-ssh/src/config.rs`
- Create: `crates/cairn-ssh/tests/config_parse_test.rs`
- Modify: `crates/cairn-ssh/src/lib.rs` (add `pub mod config;`)

- [ ] **Step 1: 테스트 fixture 준비 — `tests/fixtures/` 폴더**

```bash
mkdir -p crates/cairn-ssh/tests/fixtures
```

- [ ] **Step 2: `tests/config_parse_test.rs` — 파서 회귀 테스트**

```rust
use cairn_ssh::config::{parse_ssh_g_output, parse_host_blocks};
use std::path::PathBuf;

#[test]
fn parse_simple_ssh_g() {
    let out = "\
hostname 10.0.1.5
port 22
user deploy
identityfile ~/.ssh/id_ed25519
serveraliveinterval 60
serveralivecountmax 3
stricthostkeychecking ask
userknownhostsfile ~/.ssh/known_hosts
hashknownhosts no
compression no
";
    let cfg = parse_ssh_g_output(out).unwrap();
    assert_eq!(cfg.hostname, "10.0.1.5");
    assert_eq!(cfg.port, 22);
    assert_eq!(cfg.user, "deploy");
    assert_eq!(cfg.server_alive_interval.as_secs(), 60);
    assert!(!cfg.hash_known_hosts);
    // ~/ should expand to absolute path
    assert!(cfg.identity_files[0].starts_with(std::env::var_os("HOME").unwrap()));
}

#[test]
fn parse_proxy_command() {
    let out = "\
hostname prod-internal
port 22
user deploy
proxycommand cloudflared access ssh --hostname %h
";
    let cfg = parse_ssh_g_output(out).unwrap();
    assert_eq!(cfg.proxy_command.as_deref(), Some("cloudflared access ssh --hostname %h"));
}

#[test]
fn parse_default_fills_in() {
    // Empty output — parser should fill defaults
    let cfg = parse_ssh_g_output("hostname localhost\n").unwrap();
    assert_eq!(cfg.port, 22);
    assert_eq!(cfg.server_alive_interval.as_secs(), 30); // 0 → 30 bump
    assert_eq!(cfg.user, std::env::var("USER").unwrap_or_default());
}

#[test]
fn host_blocks_list_skips_wildcards() {
    let cfg = "\
Host prod-api staging-db
    HostName 10.0.1.5

Host *.internal
    User deploy

Host git-*
    IdentityFile ~/.ssh/git_key

Host dev-tunnel
    ProxyCommand cloudflared access ssh --hostname %h
";
    let hosts = parse_host_blocks(cfg);
    assert_eq!(hosts, vec!["prod-api", "staging-db", "dev-tunnel"]);
}
```

- [ ] **Step 3: 테스트 실행 — 실패 확인**

```bash
cargo test -p cairn-ssh --test config_parse_test
```

Expected: FAIL ("module `config` is private" or "function `parse_ssh_g_output` not found").

- [ ] **Step 4: `src/config.rs` — 파서 구현**

```rust
use crate::error::{Result, SshError};
use crate::types::{ResolvedConfig, StrictMode};
use std::collections::HashMap;
use std::path::PathBuf;
use std::process::Command;
use std::time::Duration;

/// Resolve a host alias to a fully-expanded config via `ssh -G <host>`.
/// This delegates all ssh_config semantics (ProxyCommand, ProxyJump, Match,
/// Include, wildcards) to the system OpenSSH binary.
pub fn resolve_host(host: &str) -> Result<ResolvedConfig> {
    let out = Command::new("/usr/bin/ssh")
        .arg("-G")
        .arg(host)
        .output()
        .map_err(|e| SshError::ConfigResolution {
            host: host.into(),
            msg: format!("spawn ssh -G: {e}"),
        })?;
    if !out.status.success() {
        return Err(SshError::ConfigResolution {
            host: host.into(),
            msg: String::from_utf8_lossy(&out.stderr).into_owned(),
        });
    }
    parse_ssh_g_output(&String::from_utf8_lossy(&out.stdout))
}

pub fn parse_ssh_g_output(out: &str) -> Result<ResolvedConfig> {
    let mut kv: HashMap<&str, Vec<&str>> = HashMap::new();
    for line in out.lines() {
        let line = line.trim();
        if line.is_empty() { continue; }
        let mut parts = line.splitn(2, ' ');
        let k = parts.next().unwrap_or("");
        let v = parts.next().unwrap_or("");
        kv.entry(k).or_default().push(v);
    }

    let first = |k: &str| kv.get(k).and_then(|v| v.first().copied()).unwrap_or("");
    let all = |k: &str| kv.get(k).cloned().unwrap_or_default();

    let hostname = first("hostname").to_string();
    if hostname.is_empty() {
        return Err(SshError::ConfigResolution {
            host: "<unknown>".into(),
            msg: "no hostname in output".into(),
        });
    }

    let port: u16 = first("port").parse().unwrap_or(22);
    let user = {
        let u = first("user");
        if u.is_empty() { std::env::var("USER").unwrap_or_default() } else { u.into() }
    };

    let identity_files = all("identityfile").into_iter()
        .map(|p| expand_tilde(p))
        .collect();

    let identity_agent = {
        let v = first("identityagent");
        if v.is_empty() || v.eq_ignore_ascii_case("none") {
            None
        } else if v == "SSH_AUTH_SOCK" {
            std::env::var_os("SSH_AUTH_SOCK").map(PathBuf::from)
        } else {
            Some(expand_tilde(v))
        }
    };

    let proxy_command = {
        let v = first("proxycommand").trim();
        if v.is_empty() || v.eq_ignore_ascii_case("none") { None } else { Some(v.to_string()) }
    };
    let proxy_jump = {
        let v = first("proxyjump").trim();
        if v.is_empty() || v.eq_ignore_ascii_case("none") { None } else { Some(v.to_string()) }
    };

    // Server keepalive: 0 means disabled in OpenSSH, but russh benefits from
    // an active ping. Force 30s floor.
    let sai_raw: u64 = first("serveraliveinterval").parse().unwrap_or(0);
    let server_alive_interval = Duration::from_secs(sai_raw.max(30));
    let server_alive_count_max: u32 = first("serveralivecountmax").parse().unwrap_or(3);

    let strict_host_key_checking = match first("stricthostkeychecking").to_ascii_lowercase().as_str() {
        "yes"        => StrictMode::Yes,
        "accept-new" => StrictMode::AcceptNew,
        "no" | "off" => StrictMode::No,
        _            => StrictMode::Ask,
    };

    let user_known_hosts_file = all("userknownhostsfile").into_iter()
        .flat_map(|s| s.split_whitespace().map(|p| expand_tilde(p)).collect::<Vec<_>>())
        .collect();
    let global_known_hosts_file = all("globalknownhostsfile").into_iter()
        .flat_map(|s| s.split_whitespace().map(|p| expand_tilde(p)).collect::<Vec<_>>())
        .collect();

    let host_key_algorithms = split_comma(first("hostkeyalgorithms"));
    let preferred_authentications = split_comma(first("preferredauthentications"));

    let compression = matches!(first("compression"), "yes" | "true");
    let hash_known_hosts = matches!(first("hashknownhosts"), "yes" | "true");

    Ok(ResolvedConfig {
        hostname, port, user,
        identity_files, identity_agent,
        proxy_command, proxy_jump,
        server_alive_interval, server_alive_count_max,
        strict_host_key_checking,
        user_known_hosts_file, global_known_hosts_file,
        host_key_algorithms, preferred_authentications,
        compression, hash_known_hosts,
    })
}

fn expand_tilde(p: &str) -> PathBuf {
    if let Some(rest) = p.strip_prefix("~/") {
        if let Some(home) = std::env::var_os("HOME") {
            let mut pb = PathBuf::from(home);
            pb.push(rest);
            return pb;
        }
    }
    PathBuf::from(p)
}

fn split_comma(s: &str) -> Vec<String> {
    if s.is_empty() { return Vec::new(); }
    s.split(',').map(|t| t.trim().to_string()).filter(|t| !t.is_empty()).collect()
}

/// Shallow `~/.ssh/config` parser: extracts Host block names (no wildcards).
/// Used only to populate the sidebar "Remote Hosts" section — actual
/// per-host resolution goes through `resolve_host` (ssh -G).
///
/// Follows top-level `Include` directives (depth-limited to 5).
pub fn list_configured_hosts() -> Vec<String> {
    let Some(home) = std::env::var_os("HOME") else { return Vec::new(); };
    let mut cfg_root = PathBuf::from(&home);
    cfg_root.push(".ssh/config");
    let mut seen = std::collections::HashSet::new();
    let mut buf = String::new();
    walk_includes(&cfg_root, 0, 5, &mut buf, &mut seen);
    parse_host_blocks(&buf)
}

pub fn parse_host_blocks(cfg: &str) -> Vec<String> {
    let mut out = Vec::new();
    for line in cfg.lines() {
        let trimmed = line.trim();
        if trimmed.is_empty() || trimmed.starts_with('#') { continue; }
        let lower = trimmed.to_ascii_lowercase();
        // Only top-level `Host x y z` — skip `Match host …`
        if let Some(rest) = lower.strip_prefix("host ") {
            // Use the original (non-lowered) slice for names
            let rest_orig = trimmed["Host".len()..].trim_start();
            for name in rest_orig.split_whitespace() {
                if !name.contains('*') && !name.contains('?') && !name.starts_with('!') {
                    out.push(name.to_string());
                }
            }
            let _ = rest;
        }
    }
    out
}

fn walk_includes(
    path: &PathBuf,
    depth: u8,
    max_depth: u8,
    buf: &mut String,
    seen: &mut std::collections::HashSet<PathBuf>,
) {
    if depth >= max_depth { return; }
    let canon = match std::fs::canonicalize(path) {
        Ok(p) => p,
        Err(_) => return,
    };
    if !seen.insert(canon.clone()) { return; }
    let Ok(text) = std::fs::read_to_string(&canon) else { return; };
    for line in text.lines() {
        let trimmed = line.trim();
        if let Some(rest) = trimmed.strip_prefix("Include ").or_else(|| trimmed.strip_prefix("include ")) {
            for glob in rest.split_whitespace() {
                let expanded = expand_tilde(glob);
                // Very minimal glob: support trailing `*` in filename portion.
                if let Some(parent) = expanded.parent() {
                    if let Ok(entries) = std::fs::read_dir(parent) {
                        let fname = expanded.file_name().map(|s| s.to_string_lossy().to_string()).unwrap_or_default();
                        let is_star = fname.ends_with('*');
                        let prefix = fname.trim_end_matches('*').to_string();
                        for e in entries.flatten() {
                            let ep = e.path();
                            let n = ep.file_name().map(|s| s.to_string_lossy().to_string()).unwrap_or_default();
                            let matches = if is_star { n.starts_with(&prefix) } else { n == fname };
                            if matches && ep.is_file() {
                                walk_includes(&ep, depth + 1, max_depth, buf, seen);
                            }
                        }
                    }
                } else if expanded.is_file() {
                    walk_includes(&expanded, depth + 1, max_depth, buf, seen);
                }
            }
        } else {
            buf.push_str(line);
            buf.push('\n');
        }
    }
}
```

- [ ] **Step 5: `src/lib.rs`에 모듈 추가**

```rust
pub mod config;
```

`pub use` 라인에 `pub use config::{resolve_host, list_configured_hosts};` 추가.

- [ ] **Step 6: 테스트 PASS 확인**

```bash
cargo test -p cairn-ssh --test config_parse_test
```

Expected: 4 passed.

- [ ] **Step 7: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): ssh -G config resolver + shallow host list parser

parse_ssh_g_output maps lowercase key/value lines from 'ssh -G' into
ResolvedConfig: tilde expansion for paths, SSH_AUTH_SOCK→env for
IdentityAgent, 30s floor on ServerAliveInterval, comma-split for
algorithm/auth lists. resolve_host() spawns /usr/bin/ssh and surfaces
stderr via SshError::ConfigResolution.

list_configured_hosts() shallow-parses ~/.ssh/config (+ top-level
Include, depth 5) pulling Host block names minus wildcards/negations
for the sidebar 'Remote Hosts' list. Per-host details still go through
ssh -G at connect time.

Tests cover plain/proxy/defaults/wildcard-skip paths."
```

---

## Task 3: `hostkey.rs` + `known_hosts_hash.rs` — fingerprint + match + append

**Files:**
- Create: `crates/cairn-ssh/src/hostkey.rs`
- Create: `crates/cairn-ssh/src/known_hosts_hash.rs`
- Create: `crates/cairn-ssh/tests/known_hosts_test.rs`
- Create: `crates/cairn-ssh/tests/hostkey_fingerprint_test.rs`
- Modify: `crates/cairn-ssh/src/lib.rs`
- Modify: `crates/cairn-ssh/Cargo.toml` (add `hmac = "0.12"`, `base64 = "0.22"`)

- [ ] **Step 1: deps 추가**

`crates/cairn-ssh/Cargo.toml` `[dependencies]`에:

```toml
hmac   = "0.12"
base64 = "0.22"
async-trait = "0.1"
```

- [ ] **Step 2: `tests/hostkey_fingerprint_test.rs`**

```rust
use cairn_ssh::hostkey::sha256_fingerprint;

#[test]
fn fingerprint_ed25519_known_vector() {
    // ssh-keygen -lf key.pub outputs: 256 SHA256:<base64> comment
    // This test pins the base64 output for a known ed25519 public-key blob.
    // Blob bytes taken from: ssh-keygen -g -t ed25519 → public portion
    let pubkey_blob: &[u8] = &[
        0x00, 0x00, 0x00, 0x0b, b's', b's', b'h', b'-', b'e', b'd', b'2', b'5', b'5', b'1', b'9',
        0x00, 0x00, 0x00, 0x20,
        0x45, 0x65, 0xc5, 0x9a, 0x8c, 0x5f, 0x61, 0x25, 0x8a, 0x2b, 0xfa, 0xd6, 0xcd, 0x9e, 0x8c, 0x51,
        0x14, 0x9e, 0x5f, 0x3d, 0x76, 0x2d, 0x64, 0x15, 0x05, 0x01, 0xa3, 0xfc, 0x88, 0x3c, 0x42, 0x20,
    ];
    let fp = sha256_fingerprint(pubkey_blob);
    // Manually computed via: openssl dgst -sha256 -binary | openssl base64 (stripped '=')
    assert!(fp.starts_with("SHA256:"));
    assert_eq!(fp.len(), "SHA256:".len() + 43);
}
```

- [ ] **Step 3: `tests/known_hosts_test.rs`**

```rust
use cairn_ssh::known_hosts_hash::match_hashed_entry;
use cairn_ssh::hostkey::{KnownHostsStore, KnownResult};
use std::io::Write;
use tempfile::NamedTempFile;

#[test]
fn plain_entry_match() {
    let mut f = NamedTempFile::new().unwrap();
    writeln!(f, "# comment").unwrap();
    writeln!(f, "prod-api ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIEVlxZqMX2ElimvprNnSjFEUnlz7di1kFQUBoy+IIPBC").unwrap();
    f.flush().unwrap();

    let store = KnownHostsStore::new(vec![f.path().to_path_buf()]);
    let pk = b"unused-for-this-test"; // actual match uses full PublicKey object
    match store.lookup("prod-api", 22, "ssh-ed25519", pk) {
        KnownResult::Match | KnownResult::Mismatch { .. } | KnownResult::NotFound => {}
    }
    // We assert the lookup path doesn't panic. Real match testing needs
    // a parsed PublicKey — covered in integration tests.
}

#[test]
fn hashed_entry_matches() {
    // Format: |1|<base64 salt>|<base64 HMAC-SHA1(salt, "host")>
    // Use a known salt + host to compute matching hash offline.
    let salt = b"salted12";
    let host = "prod-api";
    let sample = make_hashed_line(salt, host);
    let parts: Vec<&str> = sample.split_whitespace().collect();
    assert!(parts[0].starts_with("|1|"));
    assert!(match_hashed_entry(parts[0], host));
    assert!(!match_hashed_entry(parts[0], "other-host"));
}

fn make_hashed_line(salt: &[u8], host: &str) -> String {
    use base64::prelude::*;
    use hmac::{Hmac, Mac};
    use sha1::Sha1;
    type HmacSha1 = Hmac<Sha1>;
    let mut mac = HmacSha1::new_from_slice(salt).unwrap();
    mac.update(host.as_bytes());
    let result = mac.finalize().into_bytes();
    format!("|1|{}|{} ssh-ed25519 AAAA...", BASE64_STANDARD.encode(salt), BASE64_STANDARD.encode(result))
}
```

- [ ] **Step 4: `Cargo.toml` dev-deps에 `sha1 = "0.10"` 추가 (테스트 전용)**

```toml
[dev-dependencies]
# ... existing ...
sha1 = "0.10"
```

런타임 deps에도 `sha1 = "0.10"` 추가 (HMAC-SHA1 사용).

- [ ] **Step 5: `src/known_hosts_hash.rs`**

```rust
use base64::prelude::*;
use hmac::{Hmac, Mac};
use sha1::Sha1;

type HmacSha1 = Hmac<Sha1>;

/// Match an OpenSSH hashed known_hosts token (format `|1|<b64 salt>|<b64 hmac>`)
/// against a host name.
///
/// Returns true iff the HMAC-SHA1 of `host` under `salt` equals `mac`.
pub fn match_hashed_entry(token: &str, host: &str) -> bool {
    let rest = match token.strip_prefix("|1|") {
        Some(r) => r,
        None => return false,
    };
    let mut parts = rest.splitn(2, '|');
    let (salt_b64, mac_b64) = match (parts.next(), parts.next()) {
        (Some(s), Some(m)) => (s, m),
        _ => return false,
    };
    let salt = match BASE64_STANDARD.decode(salt_b64) {
        Ok(v) => v,
        Err(_) => return false,
    };
    let expected = match BASE64_STANDARD.decode(mac_b64) {
        Ok(v) => v,
        Err(_) => return false,
    };
    let mut mac = match HmacSha1::new_from_slice(&salt) {
        Ok(m) => m,
        Err(_) => return false,
    };
    mac.update(host.as_bytes());
    mac.verify_slice(&expected).is_ok()
}
```

- [ ] **Step 6: `src/hostkey.rs`**

```rust
use crate::known_hosts_hash::match_hashed_entry;
use async_trait::async_trait;
use base64::prelude::*;
use sha2::{Digest, Sha256};
use std::fs::{File, OpenOptions};
use std::io::{BufRead, BufReader, Write};
use std::path::PathBuf;

pub fn sha256_fingerprint(pubkey_blob: &[u8]) -> String {
    let mut h = Sha256::new();
    h.update(pubkey_blob);
    let digest = h.finalize();
    let b64 = BASE64_STANDARD_NO_PAD.encode(digest);
    format!("SHA256:{b64}")
}

#[derive(Debug, Clone, PartialEq, Eq)]
pub enum KnownResult {
    Match,
    NotFound,
    Mismatch { stored_algo: String, stored_blob: Vec<u8> },
}

#[derive(Debug, Clone)]
pub enum TofuDecision {
    Accept,
    AcceptAndSave,
    Reject,
}

#[async_trait]
pub trait HostKeyResolver: Send + Sync {
    async fn resolve(
        &self,
        host: &str,
        port: u16,
        offered_algo: &str,
        offered_blob: &[u8],
        known: KnownResult,
    ) -> TofuDecision;
}

/// Reads user+global known_hosts files and answers match queries.
/// Append writes go to the FIRST user path; hash toggle controls format.
pub struct KnownHostsStore {
    pub paths: Vec<PathBuf>,
}

impl KnownHostsStore {
    pub fn new(paths: Vec<PathBuf>) -> Self {
        Self { paths }
    }

    pub fn lookup(&self, host: &str, port: u16, offered_algo: &str, offered_blob: &[u8]) -> KnownResult {
        let target = host_key(host, port);
        let mut mismatch: Option<(String, Vec<u8>)> = None;
        for p in &self.paths {
            let Ok(f) = File::open(p) else { continue; };
            for line in BufReader::new(f).lines().map_while(Result::ok) {
                let line = line.trim();
                if line.is_empty() || line.starts_with('#') { continue; }
                let parts: Vec<&str> = line.splitn(3, ' ').collect();
                if parts.len() < 3 { continue; }
                let (hosts_field, algo, b64) = (parts[0], parts[1], parts[2]);
                if !host_matches(hosts_field, host, port, &target) { continue; }
                let blob = match BASE64_STANDARD.decode(b64.split_whitespace().next().unwrap_or("")) {
                    Ok(v) => v, Err(_) => continue,
                };
                if algo == offered_algo && blob == offered_blob {
                    return KnownResult::Match;
                } else if algo == offered_algo {
                    mismatch = Some((algo.into(), blob));
                }
            }
        }
        match mismatch {
            Some((a, b)) => KnownResult::Mismatch { stored_algo: a, stored_blob: b },
            None         => KnownResult::NotFound,
        }
    }

    pub fn append(&self, host: &str, port: u16, algo: &str, blob: &[u8], hash_known_hosts: bool) -> std::io::Result<()> {
        let path = self.paths.first().ok_or_else(|| std::io::Error::new(std::io::ErrorKind::NotFound, "no user_known_hosts_file configured"))?;
        if let Some(parent) = path.parent() {
            std::fs::create_dir_all(parent)?;
        }
        let mut f = OpenOptions::new().create(true).append(true).mode_0600().open(path)?;
        let host_field = if hash_known_hosts {
            hashed_host_field(host, port)
        } else {
            plain_host_field(host, port)
        };
        writeln!(f, "{host_field} {algo} {}", BASE64_STANDARD.encode(blob))?;
        Ok(())
    }
}

fn host_key(host: &str, port: u16) -> String {
    if port == 22 { host.into() } else { format!("[{host}]:{port}") }
}

fn host_matches(field: &str, host: &str, port: u16, target: &str) -> bool {
    // Hashed token is a whole field
    if field.starts_with("|1|") {
        return match_hashed_entry(field, target);
    }
    // Comma-separated list of patterns
    for p in field.split(',') {
        let p = p.trim();
        if p == target || p == host {
            return true;
        }
        // `[host]:port` literal compare (target already normalized)
    }
    false
}

fn plain_host_field(host: &str, port: u16) -> String { host_key(host, port) }

fn hashed_host_field(host: &str, port: u16) -> String {
    use hmac::{Hmac, Mac};
    use rand::RngCore;
    use sha1::Sha1;
    type HmacSha1 = Hmac<Sha1>;
    let mut salt = [0u8; 20];
    rand::thread_rng().fill_bytes(&mut salt);
    let mut mac = HmacSha1::new_from_slice(&salt).expect("HMAC");
    mac.update(host_key(host, port).as_bytes());
    let digest = mac.finalize().into_bytes();
    format!("|1|{}|{}", BASE64_STANDARD.encode(salt), BASE64_STANDARD.encode(digest))
}

#[cfg(unix)]
trait OpenOptionsExt600 { fn mode_0600(&mut self) -> &mut Self; }
#[cfg(unix)]
impl OpenOptionsExt600 for OpenOptions {
    fn mode_0600(&mut self) -> &mut Self {
        use std::os::unix::fs::OpenOptionsExt;
        self.mode(0o600)
    }
}
```

Add `rand = "0.8"` to `[dependencies]`.

- [ ] **Step 7: `src/lib.rs`에 module 추가**

```rust
pub mod hostkey;
pub mod known_hosts_hash;
pub use hostkey::{HostKeyResolver, KnownHostsStore, KnownResult, TofuDecision, sha256_fingerprint};
```

- [ ] **Step 8: 테스트 PASS**

```bash
cargo test -p cairn-ssh --tests
```

Expected: all passing (config_parse_test 4 + hostkey_fingerprint_test 1 + known_hosts_test 2).

- [ ] **Step 9: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): host-key fingerprint + known_hosts match/append

sha256_fingerprint() produces 'SHA256:<base64-no-pad>' matching
'ssh-keygen -l -f' format. KnownHostsStore.lookup() resolves Match /
NotFound / Mismatch over user+global known_hosts; supports plain and
|1|-hashed entries via known_hosts_hash::match_hashed_entry
(HMAC-SHA1). Append writes to first user path with 0600 permissions,
honoring HashKnownHosts toggle.

HostKeyResolver trait is the UI seam for TOFU decisions."
```

---

## Task 4: `proxy.rs` — ChildIoStream + ProxyCommand

**Files:**
- Create: `crates/cairn-ssh/src/proxy.rs`
- Create: `crates/cairn-ssh/tests/proxy_command_test.rs`
- Modify: `crates/cairn-ssh/src/lib.rs`

- [ ] **Step 1: `tests/proxy_command_test.rs` — subprocess smoke**

```rust
use cairn_ssh::proxy::dial_with_proxy;
use tokio::io::{AsyncReadExt, AsyncWriteExt};

#[tokio::test]
async fn proxy_command_echoes_stdio() {
    // Use `cat` as a trivial proxy that echoes stdin to stdout.
    let mut stream = dial_with_proxy("cat", "ignored", 0, "ignored").await.unwrap();
    stream.write_all(b"hello\n").await.unwrap();
    stream.flush().await.unwrap();
    let mut buf = [0u8; 6];
    stream.read_exact(&mut buf).await.unwrap();
    assert_eq!(&buf, b"hello\n");
}

#[tokio::test]
async fn proxy_command_spawn_failure_surfaces() {
    let err = dial_with_proxy("/nonexistent-binary-xyz-cairn-test", "h", 22, "u").await;
    assert!(err.is_err());
}

#[tokio::test]
async fn proxy_command_token_expansion() {
    // `echo %h %p %r` → subshell prints the expanded arguments.
    let mut stream = dial_with_proxy("echo %h %p %r", "prod-api", 2222, "deploy").await.unwrap();
    let mut buf = Vec::new();
    stream.read_to_end(&mut buf).await.unwrap();
    assert_eq!(String::from_utf8_lossy(&buf).trim(), "prod-api 2222 deploy");
}
```

- [ ] **Step 2: `src/proxy.rs`**

```rust
use crate::error::{Result, SshError};
use std::pin::Pin;
use std::process::Stdio;
use std::sync::{Arc, Mutex};
use std::task::{Context, Poll};
use tokio::io::{AsyncRead, AsyncWrite, ReadBuf};
use tokio::process::{Child, ChildStdin, ChildStdout, Command};

/// Expand OpenSSH ProxyCommand tokens: %h, %p, %r (host/port/user).
pub fn expand_tokens(cmd: &str, host: &str, port: u16, user: &str) -> String {
    cmd.replace("%h", host)
        .replace("%p", &port.to_string())
        .replace("%r", user)
}

/// Spawn the proxy command under /bin/sh and return an AsyncRead + AsyncWrite
/// stream composed of the child's stdout + stdin. Stderr is drained to an
/// in-memory buffer (capped at 8 KiB) for diagnostic reporting on failure.
pub async fn dial_with_proxy(
    proxy_cmd: &str,
    host: &str,
    port: u16,
    user: &str,
) -> Result<ProxyStream> {
    let expanded = expand_tokens(proxy_cmd, host, port, user);
    let mut child = Command::new("/bin/sh")
        .arg("-c").arg(&expanded)
        .stdin(Stdio::piped())
        .stdout(Stdio::piped())
        .stderr(Stdio::piped())
        .kill_on_drop(true)
        .spawn()
        .map_err(|e| SshError::ProxyCommandSpawn { cmd: expanded.clone(), source: e })?;

    let stderr_buf: Arc<Mutex<Vec<u8>>> = Arc::new(Mutex::new(Vec::new()));
    if let Some(stderr) = child.stderr.take() {
        let buf = stderr_buf.clone();
        tokio::spawn(async move {
            use tokio::io::AsyncReadExt;
            let mut r = stderr;
            let mut tmp = [0u8; 512];
            loop {
                match r.read(&mut tmp).await {
                    Ok(0) | Err(_) => break,
                    Ok(n) => {
                        let mut g = buf.lock().unwrap();
                        let room = 8192usize.saturating_sub(g.len());
                        if room == 0 { continue; }
                        let take = n.min(room);
                        g.extend_from_slice(&tmp[..take]);
                    }
                }
            }
        });
    }

    let stdout = child.stdout.take().ok_or_else(|| SshError::Russh("proxy: no stdout".into()))?;
    let stdin = child.stdin.take().ok_or_else(|| SshError::Russh("proxy: no stdin".into()))?;

    Ok(ProxyStream { child, stdout, stdin, stderr_buf })
}

pub struct ProxyStream {
    child: Child,
    stdout: ChildStdout,
    stdin: ChildStdin,
    stderr_buf: Arc<Mutex<Vec<u8>>>,
}

impl ProxyStream {
    /// Latest stderr content captured from the child, up to 8 KiB.
    pub fn stderr_snapshot(&self) -> Vec<u8> {
        self.stderr_buf.lock().unwrap().clone()
    }

    /// Wait for the child to exit and translate a non-zero status into
    /// SshError::ProxyCommandFailed with the stderr snapshot (first 500 chars).
    pub async fn ensure_running(&mut self) -> Result<()> {
        match self.child.try_wait() {
            Ok(Some(status)) if !status.success() => {
                let stderr = String::from_utf8_lossy(&self.stderr_snapshot()).into_owned();
                let mut preview = stderr;
                preview.truncate(500);
                Err(SshError::ProxyCommandFailed {
                    exit_code: status.code().unwrap_or(-1),
                    stderr_preview: preview,
                })
            }
            _ => Ok(()),
        }
    }
}

impl AsyncRead for ProxyStream {
    fn poll_read(mut self: Pin<&mut Self>, cx: &mut Context<'_>, buf: &mut ReadBuf<'_>) -> Poll<std::io::Result<()>> {
        Pin::new(&mut self.stdout).poll_read(cx, buf)
    }
}

impl AsyncWrite for ProxyStream {
    fn poll_write(mut self: Pin<&mut Self>, cx: &mut Context<'_>, buf: &[u8]) -> Poll<std::io::Result<usize>> {
        Pin::new(&mut self.stdin).poll_write(cx, buf)
    }
    fn poll_flush(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<std::io::Result<()>> {
        Pin::new(&mut self.stdin).poll_flush(cx)
    }
    fn poll_shutdown(mut self: Pin<&mut Self>, cx: &mut Context<'_>) -> Poll<std::io::Result<()>> {
        Pin::new(&mut self.stdin).poll_shutdown(cx)
    }
}
```

- [ ] **Step 3: `src/lib.rs`**

```rust
pub mod proxy;
```

- [ ] **Step 4: 테스트 PASS**

```bash
cargo test -p cairn-ssh --test proxy_command_test
```

Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): ProxyCommand subprocess + AsyncRead/Write adapter

dial_with_proxy spawns the proxy under /bin/sh -c so shell syntax
(pipes, env, quoting) works. ProxyStream composes child stdout+stdin
into a russh-compatible AsyncRead+AsyncWrite while draining stderr
into an 8 KiB buffer for failure diagnostics. kill_on_drop guarantees
we don't leak cloudflared/aws-ssm processes.

%h/%p/%r token expansion per ssh_config(5)."
```

---

## Task 5: `pool.rs` + `auth.rs` — connect / dedup / idle reap / auth

**Files:**
- Create: `crates/cairn-ssh/src/auth.rs`
- Create: `crates/cairn-ssh/src/pool.rs`
- Create: `crates/cairn-ssh/tests/pool_test.rs`
- Modify: `crates/cairn-ssh/src/lib.rs`

- [ ] **Step 1: `src/auth.rs` — `PassphraseResolver` trait + sequence**

```rust
use crate::error::{Result, SshError};
use crate::types::ResolvedConfig;
use async_trait::async_trait;
use std::path::{Path, PathBuf};

#[async_trait]
pub trait PassphraseResolver: Send + Sync {
    /// Returns the passphrase for a key file, or None if user cancels.
    async fn resolve(&self, key_path: &Path) -> Option<String>;
}

/// Ordered list of auth methods we attempt, in priority order.
pub fn planned_methods(resolved: &ResolvedConfig) -> Vec<AuthMethod> {
    let mut out = Vec::new();
    if resolved.identity_agent.is_some() || std::env::var_os("SSH_AUTH_SOCK").is_some() {
        out.push(AuthMethod::Agent);
    }
    for id in &resolved.identity_files {
        out.push(AuthMethod::KeyFile(id.clone()));
    }
    out
}

#[derive(Debug, Clone)]
pub enum AuthMethod {
    Agent,
    KeyFile(PathBuf),
}

pub fn auth_kind_not_supported(kind: &str) -> SshError {
    let k = match kind {
        "password" => "password",
        "keyboard-interactive" => "keyboard-interactive",
        _ => "unknown",
    };
    // Map to static str
    let kind_static: &'static str = match k {
        "password" => "password",
        "keyboard-interactive" => "keyboard-interactive",
        _ => "unknown",
    };
    SshError::AuthKindNotSupported { kind: kind_static }
}

/// Helper: consolidate list of tried methods into a single "tried: X, Y" string.
pub fn format_tried(methods: &[AuthMethod]) -> String {
    methods.iter().map(|m| match m {
        AuthMethod::Agent => "agent".to_string(),
        AuthMethod::KeyFile(p) => format!("key {}", p.display()),
    }).collect::<Vec<_>>().join(", ")
}

pub type Result = std::result::Result<(), SshError>;
```

- [ ] **Step 2: `src/pool.rs` — `SshPool` with dedup + idle reaper**

```rust
use crate::auth::{planned_methods, AuthMethod, PassphraseResolver};
use crate::error::{Result, SshError};
use crate::hostkey::{HostKeyResolver, KnownHostsStore, KnownResult, TofuDecision};
use crate::proxy::dial_with_proxy;
use crate::types::{ConnKey, ConnectSpec, ResolvedConfig, StrictMode};
use parking_lot::Mutex;
use russh::client::{self, Handle};
use russh::keys::*;
use std::collections::HashMap;
use std::sync::Arc;
use std::time::{Duration, Instant};

pub struct SshPool {
    inner: Arc<PoolInner>,
}

struct PoolInner {
    sessions: Mutex<HashMap<ConnKey, Entry>>,
    idle_timeout: Duration,
}

struct Entry {
    handle: Arc<Handle<CairnHandler>>,
    resolved: ResolvedConfig,
    last_activity: Mutex<Instant>,
}

impl SshPool {
    pub fn new() -> Self {
        let inner = Arc::new(PoolInner {
            sessions: Mutex::new(HashMap::new()),
            idle_timeout: Duration::from_secs(5 * 60),
        });
        // Idle reaper task
        let weak = Arc::downgrade(&inner);
        tokio::spawn(async move {
            let mut t = tokio::time::interval(Duration::from_secs(60));
            loop {
                t.tick().await;
                let Some(strong) = weak.upgrade() else { break; };
                Self::reap_idle(&strong);
            }
        });
        Self { inner }
    }

    fn reap_idle(inner: &Arc<PoolInner>) {
        let now = Instant::now();
        let mut drop_keys = Vec::new();
        {
            let sessions = inner.sessions.lock();
            for (k, e) in sessions.iter() {
                let last = *e.last_activity.lock();
                if now.duration_since(last) > inner.idle_timeout {
                    drop_keys.push(k.clone());
                }
            }
        }
        if drop_keys.is_empty() { return; }
        let mut sessions = inner.sessions.lock();
        for k in drop_keys {
            if let Some(e) = sessions.remove(&k) {
                // Drop handle → russh sends disconnect
                drop(e);
            }
        }
    }

    /// Mark activity on a key (called on every sftp op via session wrapper).
    pub fn touch(&self, key: &ConnKey) {
        let sessions = self.inner.sessions.lock();
        if let Some(e) = sessions.get(key) {
            *e.last_activity.lock() = Instant::now();
        }
    }

    pub fn get(&self, key: &ConnKey) -> Option<Arc<Handle<CairnHandler>>> {
        let sessions = self.inner.sessions.lock();
        sessions.get(key).map(|e| e.handle.clone())
    }

    pub fn disconnect(&self, key: &ConnKey) {
        let mut sessions = self.inner.sessions.lock();
        sessions.remove(key);
    }

    pub fn close_all(&self) {
        let mut sessions = self.inner.sessions.lock();
        sessions.clear();
    }

    pub fn snapshot_keys(&self) -> Vec<ConnKey> {
        self.inner.sessions.lock().keys().cloned().collect()
    }

    /// Connect (or return existing) session for the given spec.
    pub async fn connect(
        &self,
        spec: ConnectSpec,
        hostkey: Arc<dyn HostKeyResolver>,
        passphrase: Arc<dyn PassphraseResolver>,
    ) -> Result<ConnKey> {
        let mut resolved = crate::config::resolve_host(&spec.host_alias)?;
        if let Some(u) = spec.user_override { resolved.user = u; }
        if let Some(p) = spec.port_override { resolved.port = p; }
        if let Some(id) = spec.identity_file_override { resolved.identity_files = vec![id]; }
        if let Some(pc) = spec.proxy_command_override { resolved.proxy_command = Some(pc); }

        // ProxyJump without ProxyCommand is not supported in v1
        if resolved.proxy_command.is_none() && resolved.proxy_jump.is_some() {
            return Err(SshError::ProxyJumpNotSupported);
        }

        let key = ConnKey::from_resolved(&resolved);
        if self.inner.sessions.lock().contains_key(&key) {
            return Ok(key);
        }

        let handle = open_session(&resolved, hostkey, passphrase).await?;
        let entry = Entry {
            handle: Arc::new(handle),
            resolved,
            last_activity: Mutex::new(Instant::now()),
        };
        self.inner.sessions.lock().insert(key.clone(), entry);
        Ok(key)
    }
}

async fn open_session(
    resolved: &ResolvedConfig,
    hostkey_resolver: Arc<dyn HostKeyResolver>,
    passphrase_resolver: Arc<dyn PassphraseResolver>,
) -> Result<Handle<CairnHandler>> {
    let config = Arc::new(client::Config {
        keepalive_interval: Some(resolved.server_alive_interval),
        keepalive_max: resolved.server_alive_count_max as usize,
        ..Default::default()
    });

    let known_hosts_paths: Vec<_> = resolved.user_known_hosts_file.iter()
        .chain(resolved.global_known_hosts_file.iter())
        .cloned()
        .collect();
    let handler = CairnHandler {
        host: resolved.hostname.clone(),
        port: resolved.port,
        strict: resolved.strict_host_key_checking,
        hash_known_hosts: resolved.hash_known_hosts,
        store: KnownHostsStore::new(known_hosts_paths),
        resolver: hostkey_resolver,
    };

    // Connect either through ProxyCommand stream or direct TCP
    let mut handle = if let Some(pc) = &resolved.proxy_command {
        let stream = dial_with_proxy(pc, &resolved.hostname, resolved.port, &resolved.user).await?;
        client::connect_stream(config, stream, handler).await.map_err(|e| SshError::Russh(e.to_string()))?
    } else {
        let addr = (resolved.hostname.as_str(), resolved.port);
        client::connect(config, addr, handler).await.map_err(|e| SshError::Russh(e.to_string()))?
    };

    // Auth sequence
    let methods = planned_methods(resolved);
    let mut authed = false;
    for method in &methods {
        match method {
            AuthMethod::Agent => {
                let sock = resolved.identity_agent.clone()
                    .or_else(|| std::env::var_os("SSH_AUTH_SOCK").map(std::path::PathBuf::from));
                if let Some(sock) = sock {
                    if let Ok(mut agent) = russh::keys::agent::client::AgentClient::connect_uds(sock).await {
                        if let Ok(ids) = agent.request_identities().await {
                            for id in ids {
                                let (agent2, res) = handle.authenticate_future(&resolved.user, id, agent).await;
                                agent = agent2;
                                if matches!(res, Ok(true)) { authed = true; break; }
                            }
                        }
                    }
                }
            }
            AuthMethod::KeyFile(path) => {
                // Try unencrypted first
                match russh::keys::load_secret_key(path, None) {
                    Ok(key) => {
                        if handle.authenticate_publickey(&resolved.user, Arc::new(key)).await.unwrap_or(false) {
                            authed = true;
                        }
                    }
                    Err(russh::keys::Error::KeyIsEncrypted) => {
                        // Prompt for passphrase (up to 3 attempts)
                        for _ in 0..3 {
                            let Some(pw) = passphrase_resolver.resolve(path).await else {
                                break;
                            };
                            if let Ok(key) = russh::keys::load_secret_key(path, Some(&pw)) {
                                if handle.authenticate_publickey(&resolved.user, Arc::new(key)).await.unwrap_or(false) {
                                    authed = true;
                                    break;
                                }
                            }
                        }
                    }
                    Err(_) => continue,
                }
            }
        }
        if authed { break; }
    }
    if !authed {
        return Err(SshError::AuthNoMethods { tried: crate::auth::format_tried(&methods) });
    }
    Ok(handle)
}

/// russh client handler — consults HostKeyResolver + KnownHostsStore.
pub struct CairnHandler {
    host: String,
    port: u16,
    strict: StrictMode,
    hash_known_hosts: bool,
    store: KnownHostsStore,
    resolver: Arc<dyn HostKeyResolver>,
}

#[async_trait::async_trait]
impl client::Handler for CairnHandler {
    type Error = russh::Error;

    async fn check_server_key(
        &mut self,
        server_public_key: &russh::keys::key::PublicKey,
    ) -> std::result::Result<bool, Self::Error> {
        let algo = server_public_key.name();
        let blob = server_public_key.public_key_bytes();
        let known = self.store.lookup(&self.host, self.port, algo, &blob);
        match (&self.strict, &known) {
            (_, KnownResult::Match) => Ok(true),
            (StrictMode::Yes, KnownResult::NotFound | KnownResult::Mismatch { .. }) => Ok(false),
            (StrictMode::No, KnownResult::NotFound) => {
                let _ = self.store.append(&self.host, self.port, algo, &blob, self.hash_known_hosts);
                Ok(true)
            }
            (StrictMode::AcceptNew, KnownResult::NotFound) => {
                let _ = self.store.append(&self.host, self.port, algo, &blob, self.hash_known_hosts);
                Ok(true)
            }
            _ => {
                let decision = self.resolver
                    .resolve(&self.host, self.port, algo, &blob, known)
                    .await;
                match decision {
                    TofuDecision::Accept => Ok(true),
                    TofuDecision::AcceptAndSave => {
                        let _ = self.store.append(&self.host, self.port, algo, &blob, self.hash_known_hosts);
                        Ok(true)
                    }
                    TofuDecision::Reject => Ok(false),
                }
            }
        }
    }
}
```

- [ ] **Step 3: `tests/pool_test.rs` — dedup + idle reap smoke**

```rust
use cairn_ssh::types::ConnKey;

#[test]
fn connkey_dedup_by_content() {
    use cairn_ssh::types::{ResolvedConfig, StrictMode};
    use std::time::Duration;
    let base = ResolvedConfig {
        hostname: "h".into(), port: 22, user: "u".into(),
        identity_files: vec!["/k".into()],
        identity_agent: None, proxy_command: None, proxy_jump: None,
        server_alive_interval: Duration::from_secs(30),
        server_alive_count_max: 3,
        strict_host_key_checking: StrictMode::Ask,
        user_known_hosts_file: vec![], global_known_hosts_file: vec![],
        host_key_algorithms: vec![], preferred_authentications: vec![],
        compression: false, hash_known_hosts: false,
    };
    let a = ConnKey::from_resolved(&base);
    let b = ConnKey::from_resolved(&base);
    assert_eq!(a, b);

    let mut base2 = base.clone();
    base2.proxy_command = Some("cloudflared access ssh --hostname %h".into());
    let c = ConnKey::from_resolved(&base2);
    assert_ne!(a, c);
}
```

- [ ] **Step 4: `src/lib.rs` 갱신**

```rust
pub mod auth;
pub mod pool;
pub use pool::SshPool;
pub use auth::PassphraseResolver;
```

- [ ] **Step 5: 빌드 + 테스트**

```bash
cargo build -p cairn-ssh
cargo test -p cairn-ssh --test pool_test
```

Expected: `cargo build` PASS, `pool_test` 1 passed.

- [ ] **Step 6: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): connection pool + auth sequence (agent + key + passphrase)

SshPool dedup sessions by ConnKey (sha256 of proxy/identity/agent/algo
fields) so multiple tabs to the same host share one russh handle.
Background tokio task reaps sessions idle > 5 minutes (tick every
60s). Pool.touch() is called on every SFTP op to reset the timer.

open_session() walks planned_methods (agent first via SSH_AUTH_SOCK
or resolved IdentityAgent, then each IdentityFile in order).
Encrypted keys trigger up to 3 passphrase prompts via
PassphraseResolver. AuthNoMethods error surfaces the tried list.

CairnHandler implements russh::client::Handler.check_server_key
consulting KnownHostsStore + HostKeyResolver; honors
StrictHostKeyChecking {Yes, AcceptNew, Ask, No}."
```

---

## Task 6: `sftp.rs` — list/stat/mkdir/rename/unlink + copy-data detection

**Files:**
- Create: `crates/cairn-ssh/src/sftp.rs`
- Modify: `crates/cairn-ssh/src/lib.rs`

- [ ] **Step 1: `src/sftp.rs` — SftpHandle wrapper**

```rust
use crate::error::{Result, SshError};
use crate::pool::{CairnHandler, SshPool};
use crate::types::ConnKey;
use russh::client::Handle;
use russh_sftp::client::SftpSession;
use russh_sftp::protocol::{FileAttributes, OpenFlags};
use std::sync::Arc;

pub struct SftpHandle {
    pool: Arc<SshPool>,
    key: ConnKey,
    sftp: Arc<SftpSession>,
    supports_copy_data: bool,
}

#[derive(Debug, Clone)]
pub struct RemoteEntry {
    pub name: String,
    pub is_dir: bool,
    pub size: u64,
    pub mtime: i64,
    pub mode: u32,
}

#[derive(Debug, Clone)]
pub struct RemoteStat {
    pub size: u64,
    pub mtime: i64,
    pub mode: u32,
    pub is_dir: bool,
}

impl SftpHandle {
    pub async fn open(pool: Arc<SshPool>, key: ConnKey) -> Result<Self> {
        let handle = pool.get(&key).ok_or_else(|| SshError::ConnectionLost { host: key.hostname.clone() })?;
        let channel = handle.channel_open_session().await.map_err(|e| SshError::Russh(e.to_string()))?;
        channel.request_subsystem(true, "sftp").await.map_err(|e| SshError::Russh(e.to_string()))?;
        let sftp = SftpSession::new(channel.into_stream()).await
            .map_err(|e| SshError::SftpProtocol(e.to_string()))?;
        let supports_copy_data = sftp.extensions()
            .iter()
            .any(|ext| ext.name == "copy-data" || ext.name == "copy-data@openssh.com");
        Ok(Self { pool, key, sftp: Arc::new(sftp), supports_copy_data })
    }

    pub fn supports_server_side_copy(&self) -> bool { self.supports_copy_data }

    pub async fn list(&self, path: &str) -> Result<Vec<RemoteEntry>> {
        self.pool.touch(&self.key);
        let mut dir = self.sftp.read_dir(path).await.map_err(map_sftp)?;
        let mut out = Vec::new();
        while let Some(entry) = dir.next().await.transpose().map_err(map_sftp)? {
            let name = entry.file_name();
            if name == "." || name == ".." { continue; }
            let attrs = entry.metadata();
            out.push(RemoteEntry {
                name: name.into(),
                is_dir: attrs.is_dir(),
                size: attrs.size.unwrap_or(0),
                mtime: attrs.mtime.unwrap_or(0) as i64,
                mode: attrs.permissions.unwrap_or(0),
            });
        }
        Ok(out)
    }

    pub async fn stat(&self, path: &str) -> Result<RemoteStat> {
        self.pool.touch(&self.key);
        let a = self.sftp.metadata(path).await.map_err(map_sftp)?;
        Ok(RemoteStat {
            size: a.size.unwrap_or(0),
            mtime: a.mtime.unwrap_or(0) as i64,
            mode: a.permissions.unwrap_or(0),
            is_dir: a.is_dir(),
        })
    }

    pub async fn mkdir(&self, path: &str) -> Result<()> {
        self.pool.touch(&self.key);
        self.sftp.create_dir(path).await.map_err(map_sftp)
    }

    pub async fn rename(&self, from: &str, to: &str) -> Result<()> {
        self.pool.touch(&self.key);
        self.sftp.rename(from, to).await.map_err(map_sftp)
    }

    pub async fn unlink(&self, path: &str) -> Result<()> {
        self.pool.touch(&self.key);
        // Try file first, fall back to dir
        match self.sftp.remove_file(path).await {
            Ok(()) => Ok(()),
            Err(_) => self.sftp.remove_dir(path).await.map_err(map_sftp),
        }
    }

    pub async fn read_head(&self, path: &str, max: u32) -> Result<Vec<u8>> {
        self.pool.touch(&self.key);
        let mut f = self.sftp.open_with_flags(path, OpenFlags::READ).await.map_err(map_sftp)?;
        let mut buf = vec![0u8; max as usize];
        use tokio::io::AsyncReadExt;
        let n = f.read(&mut buf).await.map_err(|e| SshError::Io(e))?;
        buf.truncate(n);
        Ok(buf)
    }

    pub(crate) fn raw(&self) -> Arc<SftpSession> { self.sftp.clone() }
    pub(crate) fn touch(&self) { self.pool.touch(&self.key); }
}

fn map_sftp(e: russh_sftp::protocol::StatusCode) -> SshError {
    use russh_sftp::protocol::StatusCode;
    match e {
        StatusCode::PermissionDenied => SshError::SftpPermissionDenied("permission denied".into()),
        StatusCode::NoSuchFile       => SshError::SftpNotFound("no such file".into()),
        StatusCode::Failure          => SshError::SftpProtocol("failure".into()),
        other                        => SshError::SftpProtocol(format!("{other:?}")),
    }
}
```

> Note: Exact russh-sftp API method names may differ across 2.x patch releases. The first implementation pass should `cargo doc --open -p russh-sftp` to confirm method signatures and adjust naming where needed; the shapes above are the contract this task must satisfy.

- [ ] **Step 2: `src/lib.rs` 갱신**

```rust
pub mod sftp;
pub use sftp::{SftpHandle, RemoteEntry, RemoteStat};
```

- [ ] **Step 3: 빌드 검증**

```bash
cargo build -p cairn-ssh
```

Expected: PASS. If russh-sftp API signatures differ, fix this file only (not the trait shape).

- [ ] **Step 4: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): SftpHandle — list/stat/mkdir/rename/unlink/read_head

Wraps russh-sftp SftpSession; pool.touch() on every op to reset the
idle timer. list() filters '.' and '..' to keep call sites simple.
unlink() tries remove_file then remove_dir so the Swift side has a
single 'delete path' verb. read_head() is the text-preview entrypoint
(bounded read up to max bytes).

supports_server_side_copy probes the sftp-session extensions list for
copy-data (OpenSSH 8.8+) so the Swift paste layer can choose between
server-side vs client-mediated copy."
```

---

## Task 7: `transfer.rs` — upload/download with progress + cancel

**Files:**
- Create: `crates/cairn-ssh/src/transfer.rs`
- Modify: `crates/cairn-ssh/src/sftp.rs` (add `upload` / `download` / `server_side_copy` methods)
- Modify: `crates/cairn-ssh/src/lib.rs`

- [ ] **Step 1: `src/transfer.rs` — CancelFlag + ProgressSink**

```rust
use std::sync::atomic::{AtomicBool, Ordering};
use std::sync::Arc;

#[derive(Clone, Default)]
pub struct CancelFlag(Arc<AtomicBool>);

impl CancelFlag {
    pub fn new() -> Self { Self::default() }
    pub fn is_cancelled(&self) -> bool { self.0.load(Ordering::Relaxed) }
    pub fn cancel(&self) { self.0.store(true, Ordering::Relaxed); }
}

/// Callback hook for progress updates. Called from the transfer loop at
/// most once per 256 KiB chunk. Implementations should be cheap — FFI
/// callers typically post to a Swift actor.
pub type ProgressSink = Arc<dyn Fn(u64) + Send + Sync>;
```

- [ ] **Step 2: `src/sftp.rs` — add transfer methods**

Append to `impl SftpHandle`:

```rust
    pub async fn download(
        &self,
        remote_path: &str,
        local_path: &std::path::Path,
        progress: crate::transfer::ProgressSink,
        cancel: crate::transfer::CancelFlag,
    ) -> Result<()> {
        use russh_sftp::protocol::OpenFlags;
        use tokio::io::{AsyncReadExt, AsyncWriteExt};
        self.pool.touch(&self.key);
        let mut f = self.sftp.open_with_flags(remote_path, OpenFlags::READ).await.map_err(map_sftp)?;
        if let Some(parent) = local_path.parent() { let _ = tokio::fs::create_dir_all(parent).await; }
        let mut dest = tokio::fs::File::create(local_path).await.map_err(SshError::Io)?;
        let mut buf = vec![0u8; 256 * 1024];
        let mut total: u64 = 0;
        loop {
            if cancel.is_cancelled() { return Err(SshError::Cancelled); }
            let n = f.read(&mut buf).await.map_err(SshError::Io)?;
            if n == 0 { break; }
            dest.write_all(&buf[..n]).await.map_err(SshError::Io)?;
            total += n as u64;
            (progress)(total);
            self.pool.touch(&self.key);
        }
        dest.flush().await.map_err(SshError::Io)?;
        Ok(())
    }

    pub async fn upload(
        &self,
        local_path: &std::path::Path,
        remote_path: &str,
        progress: crate::transfer::ProgressSink,
        cancel: crate::transfer::CancelFlag,
    ) -> Result<()> {
        use russh_sftp::protocol::OpenFlags;
        use tokio::io::{AsyncReadExt, AsyncWriteExt};
        self.pool.touch(&self.key);
        let mut src = tokio::fs::File::open(local_path).await.map_err(SshError::Io)?;
        let flags = OpenFlags::WRITE | OpenFlags::CREATE | OpenFlags::TRUNCATE;
        let mut dest = self.sftp.open_with_flags(remote_path, flags).await.map_err(map_sftp)?;
        let mut buf = vec![0u8; 256 * 1024];
        let mut total: u64 = 0;
        loop {
            if cancel.is_cancelled() { return Err(SshError::Cancelled); }
            let n = src.read(&mut buf).await.map_err(SshError::Io)?;
            if n == 0 { break; }
            dest.write_all(&buf[..n]).await.map_err(|e| SshError::Io(std::io::Error::new(std::io::ErrorKind::Other, format!("{e:?}"))))?;
            total += n as u64;
            (progress)(total);
            self.pool.touch(&self.key);
        }
        Ok(())
    }

    pub async fn server_side_copy(&self, from: &str, to: &str) -> Result<()> {
        if !self.supports_copy_data {
            return Err(SshError::SftpProtocol("copy-data extension not supported by server".into()));
        }
        self.pool.touch(&self.key);
        // russh-sftp exposes copy via extension call; exact method name
        // TBD at implementation. Fallback is SftpProtocol error so caller
        // can route to client-mediated copy.
        self.sftp.copy(from, to).await.map_err(|e| SshError::SftpProtocol(format!("{e:?}")))
    }
```

- [ ] **Step 3: `src/lib.rs` 갱신**

```rust
pub mod transfer;
pub use transfer::{CancelFlag, ProgressSink};
```

- [ ] **Step 4: 빌드**

```bash
cargo build -p cairn-ssh
```

Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "feat(ssh): download/upload with progress callbacks + cancel flag

256 KiB chunked loop checks CancelFlag before each read and calls
ProgressSink with running total. Cancel throws SshError::Cancelled;
caller decides whether to leave the partial file in place (v1 policy:
leave, so the user can see what state things are in).

server_side_copy() attempts the SFTP copy-data extension; returns
SftpProtocol error on unsupported servers so the Swift layer can
route to client-mediated download+upload fallback."
```

---

## Task 8: FFI bridge — `cairn-ffi/src/ssh.rs`

**Files:**
- Create: `crates/cairn-ffi/src/ssh.rs`
- Modify: `crates/cairn-ffi/src/lib.rs`
- Modify: `crates/cairn-ffi/Cargo.toml` (add `cairn-ssh` dep)
- Regenerate: `apps/Sources/Generated/cairn_ffi.swift` (via `scripts/gen-bindings.sh`)

- [ ] **Step 1: `crates/cairn-ffi/Cargo.toml` 에 dep 추가**

```toml
cairn-ssh = { path = "../cairn-ssh" }
tokio     = { workspace = true }
```

- [ ] **Step 2: `crates/cairn-ffi/src/ssh.rs` — swift-bridge surface**

```rust
use cairn_ssh as ssh;
use parking_lot::Mutex;
use std::path::PathBuf;
use std::sync::Arc;

#[swift_bridge::bridge]
mod ffi {
    #[swift_bridge(swift_repr = "struct")]
    struct ResolvedConfigBridge {
        hostname: String,
        port: u16,
        user: String,
        identity_files: Vec<String>,
        identity_agent: Option<String>,
        proxy_command: Option<String>,
        proxy_jump: Option<String>,
        server_alive_interval_secs: u64,
        strict_host_key_checking: String,      // "yes" | "accept-new" | "ask" | "no"
        user_known_hosts_file: Vec<String>,
        global_known_hosts_file: Vec<String>,
        hash_known_hosts: bool,
    }

    #[swift_bridge(swift_repr = "struct")]
    struct FileEntryBridge {
        name: String,
        is_dir: bool,
        size: u64,
        mtime: i64,
        mode: u32,
    }

    #[swift_bridge(swift_repr = "struct")]
    struct FileStatBridge {
        size: u64,
        mtime: i64,
        mode: u32,
        is_dir: bool,
    }

    #[swift_bridge(swift_repr = "struct")]
    struct ConnKeyBridge {
        user: String,
        hostname: String,
        port: u16,
        config_hash_hex: String,   // 32 hex chars
    }

    #[swift_bridge(swift_repr = "struct")]
    struct HostKeyOffer {
        algorithm: String,
        blob_base64: String,
        fingerprint: String,       // "SHA256:..."
    }

    #[swift_bridge(swift_repr = "struct")]
    struct ConnectSpecBridge {
        host_alias: String,
        user_override: Option<String>,
        port_override: Option<u16>,
        identity_file_override: Option<String>,
        proxy_command_override: Option<String>,
    }

    extern "Rust" {
        type SshPoolBridge;
        type SftpHandleBridge;
        type CancelFlagBridge;

        fn ssh_pool_new() -> SshPoolBridge;
        fn ssh_pool_list_configured_hosts() -> Vec<String>;
        fn ssh_pool_resolve_config(host: &str) -> Result<ResolvedConfigBridge, String>;
        fn ssh_pool_connect(
            pool: &SshPoolBridge,
            spec: ConnectSpecBridge,
        ) -> Result<ConnKeyBridge, String>;
        fn ssh_pool_disconnect(pool: &SshPoolBridge, key: ConnKeyBridge);
        fn ssh_pool_close_all(pool: &SshPoolBridge);
        fn ssh_pool_snapshot_keys(pool: &SshPoolBridge) -> Vec<ConnKeyBridge>;

        fn ssh_open_sftp(pool: &SshPoolBridge, key: ConnKeyBridge) -> Result<SftpHandleBridge, String>;
        fn sftp_list(h: &SftpHandleBridge, path: &str) -> Result<Vec<FileEntryBridge>, String>;
        fn sftp_stat(h: &SftpHandleBridge, path: &str) -> Result<FileStatBridge, String>;
        fn sftp_mkdir(h: &SftpHandleBridge, path: &str) -> Result<(), String>;
        fn sftp_rename(h: &SftpHandleBridge, from: &str, to: &str) -> Result<(), String>;
        fn sftp_unlink(h: &SftpHandleBridge, path: &str) -> Result<(), String>;
        fn sftp_read_head(h: &SftpHandleBridge, path: &str, max: u32) -> Result<Vec<u8>, String>;
        fn sftp_server_side_copy(h: &SftpHandleBridge, from: &str, to: &str) -> Result<(), String>;

        fn cancel_flag_new() -> CancelFlagBridge;
        fn cancel_flag_cancel(f: &CancelFlagBridge);

        fn sftp_download_sync(
            h: &SftpHandleBridge,
            remote: &str,
            local: &str,
            cancel: &CancelFlagBridge,
        ) -> Result<(), String>;
        fn sftp_upload_sync(
            h: &SftpHandleBridge,
            local: &str,
            remote: &str,
            cancel: &CancelFlagBridge,
        ) -> Result<(), String>;
        fn sftp_progress_poll(h: &SftpHandleBridge) -> u64;  // last reported bytes
    }

    extern "Swift" {
        type HostKeyCallback;
        fn ask_host_key(cb: &HostKeyCallback, host: String, port: u16, offer: HostKeyOffer, known_state: String) -> String;
        //   known_state: "match" | "not_found" | "mismatch"
        //   returns: "accept" | "accept_save" | "reject"

        type PassphraseCallback;
        fn ask_passphrase(cb: &PassphraseCallback, key_path: String) -> Option<String>;
    }
}

pub struct SshPoolBridge { inner: Arc<ssh::SshPool> }
pub struct SftpHandleBridge { inner: ssh::SftpHandle, progress: Arc<std::sync::atomic::AtomicU64> }
pub struct CancelFlagBridge { inner: ssh::CancelFlag }

// Singletons — Swift owns one of each per AppModel.
fn runtime() -> &'static tokio::runtime::Runtime {
    use std::sync::OnceLock;
    static RT: OnceLock<tokio::runtime::Runtime> = OnceLock::new();
    RT.get_or_init(|| tokio::runtime::Builder::new_multi_thread().enable_all().build().unwrap())
}

fn ssh_pool_new() -> SshPoolBridge {
    let pool = runtime().block_on(async { ssh::SshPool::new() });
    SshPoolBridge { inner: Arc::new(pool) }
}

fn ssh_pool_list_configured_hosts() -> Vec<String> {
    ssh::config::list_configured_hosts()
}

fn ssh_pool_resolve_config(host: &str) -> Result<ffi::ResolvedConfigBridge, String> {
    ssh::config::resolve_host(host).map(Into::into).map_err(|e| e.to_string())
}

// TODO(ffi): HostKeyCallback / PassphraseCallback are plumbed from Swift
// at connect() time — concrete threading: rustside runs in runtime().block_on,
// callback invokes ask_host_key which must not deadlock. Pattern: Swift side
// uses a synchronous NSAlert on a dispatch queue, blocking the FFI thread
// briefly. For v1 we accept that; M2 queue manager requires async callbacks.
//
// See Task 12 for the Swift-side resolver actor that implements these.
```

> **Implementer note:** The exact swift-bridge idioms for `extern "Swift"` callback types and async blocking callbacks evolve between versions. Start by copy-paste from existing `crates/cairn-ffi/src/git.rs` callback patterns, then adapt. The surface above is the contract; field names must match.

- [ ] **Step 3: `crates/cairn-ffi/src/lib.rs`**

```rust
pub mod ssh;
```

- [ ] **Step 4: swift-bridge 바인딩 재생성**

```bash
./scripts/gen-bindings.sh
```

Expected: `apps/Sources/Generated/cairn_ffi.swift` 에 `SshPoolBridge`, `SftpHandleBridge`, `ConnKeyBridge` 등 신규 타입 생성.

- [ ] **Step 5: Rust 빌드 + Swift 전체 빌드 확인**

```bash
make swift 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED. Swift는 아직 새 FFI 타입들을 안 쓰지만, 생성된 바인딩이 컴파일은 돼야 함.

- [ ] **Step 6: Commit**

```bash
git add crates/cairn-ffi/ apps/Sources/Generated/
git commit -m "feat(ffi): swift-bridge surface for cairn-ssh pool + SFTP

Exposes SshPoolBridge, SftpHandleBridge, CancelFlagBridge plus
resolve_host / list_configured_hosts / connect / disconnect /
open_sftp + SFTP ops (list, stat, mkdir, rename, unlink, read_head,
server_side_copy) + download/upload streaming with external cancel
flag and progress polling.

HostKeyCallback and PassphraseCallback are declared extern \"Swift\"
for the TOFU and passphrase prompts; concrete Swift implementations
land in Task 12.

gen-bindings.sh regenerated Swift surface."
```

---

## Task 9: `FileSystemProvider` protocol + `LocalFileSystemProvider`

**Files:**
- Create: `apps/Sources/Services/FileSystemProvider.swift`
- Create: `apps/Sources/Services/LocalFileSystemProvider.swift`
- Create: `apps/Sources/Services/FSPath.swift`
- Create: `apps/CairnTests/FileSystemProviderTests.swift`
- Modify: `apps/project.yml` (include new sources)

- [ ] **Step 1: `FSPath.swift` — path + provider identifier**

```swift
import Foundation

/// Uniquely identifies a FileSystemProvider instance. Used as a hashable
/// key across Tab/FolderModel/TransferJob so two tabs on the same SSH host
/// route to the same provider instance.
enum ProviderID: Hashable, Codable {
    case local
    case ssh(SshTarget)
}

struct SshTarget: Hashable, Codable {
    let user: String
    let hostname: String
    let port: UInt16
    let configHashHex: String        // mirrors Rust ConnKey.config_hash
}

/// Abstract path on a provider. `path` is POSIX-style absolute for both
/// local ("/Users/cyj/...") and SSH ("/var/log/nginx").
struct FSPath: Hashable, Codable {
    let provider: ProviderID
    let path: String

    var lastComponent: String {
        (path as NSString).lastPathComponent
    }

    func appending(_ component: String) -> FSPath {
        let base = path.hasSuffix("/") ? path : path + "/"
        return FSPath(provider: provider, path: base + component)
    }

    func parent() -> FSPath? {
        if path == "/" || path.isEmpty { return nil }
        let parent = (path as NSString).deletingLastPathComponent
        return FSPath(provider: provider, path: parent.isEmpty ? "/" : parent)
    }

    var isLocal: Bool { if case .local = provider { return true } else { return false } }
}
```

- [ ] **Step 2: `FileSystemProvider.swift` — protocol**

```swift
import Foundation

struct FileStat {
    let size: Int64
    let mtime: Date?
    let mode: UInt32
    let isDirectory: Bool
}

protocol FileSystemProvider: AnyObject {
    var identifier: ProviderID { get }
    var displayScheme: String? { get }         // nil for local
    var supportsServerSideCopy: Bool { get }

    func list(_ path: FSPath) async throws -> [FileEntry]
    func stat(_ path: FSPath) async throws -> FileStat
    func mkdir(_ path: FSPath) async throws
    func rename(from: FSPath, to: FSPath) async throws
    func delete(_ paths: [FSPath]) async throws

    func copyInPlace(from: FSPath, to: FSPath) async throws

    /// Small head read for preview pane — bounded by `max` bytes.
    func readHead(_ path: FSPath, max: Int) async throws -> Data

    /// Full download to a local cache URL (for Quick Look / Open With).
    /// Providers may return the source URL directly when already local.
    func downloadToCache(_ path: FSPath) async throws -> URL

    /// Upload a local URL to remote path (cross-provider transfer entry).
    /// Progress and cancel are surfaced via `TransferJob` (not here).
    func uploadFromLocal(_ localURL: URL, to remotePath: FSPath, progress: (Int64) -> Void, cancel: CancelToken) async throws
    func downloadToLocal(_ remotePath: FSPath, toLocalURL: URL, progress: (Int64) -> Void, cancel: CancelToken) async throws
}

/// Swift-side cancel token mirroring Rust's CancelFlag.
final class CancelToken {
    private let lock = NSLock()
    private var cancelled = false
    var isCancelled: Bool { lock.lock(); defer { lock.unlock() }; return cancelled }
    func cancel() { lock.lock(); cancelled = true; lock.unlock() }
}
```

- [ ] **Step 3: `LocalFileSystemProvider.swift` — pass-through over CairnEngine/FileManager**

```swift
import Foundation

final class LocalFileSystemProvider: FileSystemProvider {
    let identifier: ProviderID = .local
    let displayScheme: String? = nil
    let supportsServerSideCopy: Bool = true

    private let engine: CairnEngine

    init(engine: CairnEngine) { self.engine = engine }

    func list(_ path: FSPath) async throws -> [FileEntry] {
        try await engine.listDirectory(URL(fileURLWithPath: path.path))
    }

    func stat(_ path: FSPath) async throws -> FileStat {
        let url = URL(fileURLWithPath: path.path)
        let values = try url.resourceValues(forKeys: [.fileSizeKey, .contentModificationDateKey, .isDirectoryKey])
        let mode = (try? FileManager.default.attributesOfItem(atPath: path.path)[.posixPermissions] as? NSNumber)?.uint32Value ?? 0
        return FileStat(
            size: Int64(values.fileSize ?? 0),
            mtime: values.contentModificationDate,
            mode: mode,
            isDirectory: values.isDirectory ?? false
        )
    }

    func mkdir(_ path: FSPath) async throws {
        try FileManager.default.createDirectory(atPath: path.path, withIntermediateDirectories: false)
    }

    func rename(from: FSPath, to: FSPath) async throws {
        try FileManager.default.moveItem(atPath: from.path, toPath: to.path)
    }

    func delete(_ paths: [FSPath]) async throws {
        for p in paths {
            let url = URL(fileURLWithPath: p.path)
            try FileManager.default.trashItem(at: url, resultingItemURL: nil)
        }
    }

    func copyInPlace(from: FSPath, to: FSPath) async throws {
        try FileManager.default.copyItem(atPath: from.path, toPath: to.path)
    }

    func readHead(_ path: FSPath, max: Int) async throws -> Data {
        let url = URL(fileURLWithPath: path.path)
        let h = try FileHandle(forReadingFrom: url)
        defer { try? h.close() }
        return try h.read(upToCount: max) ?? Data()
    }

    func downloadToCache(_ path: FSPath) async throws -> URL {
        URL(fileURLWithPath: path.path)    // local: already accessible
    }

    func uploadFromLocal(_ localURL: URL, to remotePath: FSPath, progress: (Int64) -> Void, cancel: CancelToken) async throws {
        // Local → local "upload" = copy
        try FileManager.default.copyItem(at: localURL, to: URL(fileURLWithPath: remotePath.path))
    }

    func downloadToLocal(_ remotePath: FSPath, toLocalURL: URL, progress: (Int64) -> Void, cancel: CancelToken) async throws {
        try FileManager.default.copyItem(at: URL(fileURLWithPath: remotePath.path), to: toLocalURL)
    }
}
```

- [ ] **Step 4: `apps/CairnTests/FileSystemProviderTests.swift`**

```swift
import XCTest
@testable import Cairn

final class FileSystemProviderTests: XCTestCase {
    func testLocalProviderListsTempDirectory() async throws {
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
        try FileManager.default.createDirectory(at: tmp, withIntermediateDirectories: true)
        defer { try? FileManager.default.removeItem(at: tmp) }

        try "hello".write(to: tmp.appendingPathComponent("a.txt"), atomically: true, encoding: .utf8)
        let p = LocalFileSystemProvider(engine: CairnEngine())
        let entries = try await p.list(FSPath(provider: .local, path: tmp.path))
        XCTAssertEqual(entries.count, 1)
    }

    func testFSPathParentAndAppending() {
        let root = FSPath(provider: .local, path: "/Users/cyj")
        XCTAssertEqual(root.appending("Projects").path, "/Users/cyj/Projects")
        XCTAssertEqual(root.parent()?.path, "/Users")
    }
}
```

- [ ] **Step 5: `apps/project.yml` 갱신**

xcodegen sources 섹션에 신규 Swift 파일들이 이미 `apps/Sources/**/*.swift` glob에 포함돼 있다면 변경 없음. xcodegen 재생성:

```bash
cd apps && xcodegen generate
```

- [ ] **Step 6: 빌드 + 테스트**

```bash
make swift && xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | tail -30
```

Expected: test 2 passed (FileSystemProviderTests), 기존 60+ tests 통과 유지.

- [ ] **Step 7: Commit**

```bash
git add apps/
git commit -m "feat(providers): FileSystemProvider protocol + LocalFileSystemProvider

FSPath decouples paths from URL: ProviderID + POSIX string. Protocol
defines the operations each provider supports (list/stat/mkdir/rename/
delete/copyInPlace/readHead/downloadToCache + cross-provider
uploadFromLocal/downloadToLocal).

LocalFileSystemProvider is a pass-through over CairnEngine and
FileManager — existing local behavior unchanged (trash instead of
permanent delete; Finder semantics).

CancelToken is the Swift mirror of Rust's CancelFlag."
```

---

## Task 10: `FolderModel` + `Tab` FSPath 마이그레이션

**Files:**
- Modify: `apps/Sources/ViewModels/FolderModel.swift`
- Modify: `apps/Sources/App/Tab.swift`
- Modify: `apps/Sources/App/NavigationHistory.swift`
- Modify: `apps/Sources/ContentView.swift`
- Modify: `apps/Sources/App/WindowSceneModel.swift`
- Modify: callers (`SidebarModel`, `BreadcrumbBar`, `FileListCoordinator`, `FileListView`, `CommandPaletteModel`)

> **Critical path risk.** Local regression is the top risk in the spec. After this task runs, ALL existing local tests must still pass. The LocalFileSystemProvider pass-through from Task 9 is the guarantee.

- [ ] **Step 1: `NavigationHistory` generic over path-like**

```swift
// NavigationHistory.swift — change stack from [URL] to [FSPath]
struct NavigationHistory: Equatable {
    private(set) var stack: [FSPath] = []
    private(set) var index: Int = -1

    var current: FSPath? {
        guard index >= 0, index < stack.count else { return nil }
        return stack[index]
    }

    var canGoBack: Bool { index > 0 }
    var canGoForward: Bool { index >= 0 && index < stack.count - 1 }

    mutating func push(_ p: FSPath) {
        if index < stack.count - 1 {
            stack.removeSubrange((index + 1)..<stack.count)
        }
        stack.append(p)
        index = stack.count - 1
    }

    @discardableResult
    mutating func goBack() -> FSPath? {
        guard canGoBack else { return nil }
        index -= 1
        return stack[index]
    }

    @discardableResult
    mutating func goForward() -> FSPath? {
        guard canGoForward else { return nil }
        index += 1
        return stack[index]
    }
}
```

- [ ] **Step 2: `FolderModel.load(path:)` variant**

Modify `FolderModel.swift`:

```swift
@MainActor
func load(_ path: FSPath, via provider: FileSystemProvider) async {
    state = .loading
    currentPath = path
    do {
        let list = try await provider.list(path)
        entries = list
        _sortedCache = nil
        state = .loaded
    } catch {
        entries = []
        _sortedCache = nil
        state = .failed(ErrorMessage.userFacing(error))
    }
}
```

Keep the existing `load(_:URL)` as a thin adapter for callers that still hand URLs (migrate them in subsequent steps):

```swift
@MainActor
func load(_ url: URL) async {
    let p = FSPath(provider: .local, path: url.path)
    await load(p, via: LocalFileSystemProvider(engine: engine))
}
```

Add `private(set) var currentPath: FSPath?` alongside `currentFolder: URL?` (keep both during migration).

- [ ] **Step 3: `Tab` gets a `provider` + `FSPath` history**

Modify `Tab.swift`:

```swift
@Observable
final class Tab: Identifiable {
    let id = UUID()
    let folder: FolderModel
    let search: SearchModel
    let preview: PreviewModel
    let undoManager = UndoManager()

    private(set) var index: IndexService?
    private var folderWatcher: FolderWatcher?
    private(set) var git: GitService?

    var history = NavigationHistory()
    private(set) var provider: FileSystemProvider

    private(set) var currentEntry: BookmarkEntry?
    private let bookmarks: BookmarkStore
    private let engine: CairnEngine
    private var servicesTask: Task<Void, Never>?

    init(engine: CairnEngine, bookmarks: BookmarkStore, initialURL: URL) {
        self.engine = engine
        self.bookmarks = bookmarks
        self.folder = FolderModel(engine: engine)
        self.search = SearchModel(engine: engine)
        self.preview = PreviewModel(engine: engine)
        self.provider = LocalFileSystemProvider(engine: engine)
        self.history.push(FSPath(provider: .local, path: initialURL.path))
        rebuildServices(for: initialURL)
    }

    /// New SSH-aware initializer.
    init(engine: CairnEngine, bookmarks: BookmarkStore, initialPath: FSPath, provider: FileSystemProvider) {
        self.engine = engine
        self.bookmarks = bookmarks
        self.folder = FolderModel(engine: engine)
        self.search = SearchModel(engine: engine)
        self.preview = PreviewModel(engine: engine)
        self.provider = provider
        self.history.push(initialPath)
        if case .local = provider.identifier {
            rebuildServices(for: URL(fileURLWithPath: initialPath.path))
        }
        // SSH tabs skip local index/git services — see Task 11 integration.
    }

    var currentPath: FSPath? { history.current }
    var currentFolder: URL? {
        guard let p = history.current, case .local = p.provider else { return nil }
        return URL(fileURLWithPath: p.path)
    }

    // Update navigate/goBack/goForward/goUp to use FSPath
    func navigate(to path: FSPath) {
        // Scoped-access handling remains local-only; remote paths skip it.
        if case .local = path.provider, let prev = currentEntry {
            bookmarks.stopAccessing(prev)
            currentEntry = nil
        }
        history.push(path)
        rebuildProviderServices(for: path)
    }

    func navigate(to url: URL) {
        navigate(to: FSPath(provider: .local, path: url.path))
    }

    func goUp() {
        guard let cur = currentPath, let parent = cur.parent() else { return }
        navigate(to: parent)
    }

    @discardableResult
    func goBack() -> FSPath? {
        guard let p = history.goBack() else { return nil }
        rebuildProviderServices(for: p)
        return p
    }

    @discardableResult
    func goForward() -> FSPath? {
        guard let p = history.goForward() else { return nil }
        rebuildProviderServices(for: p)
        return p
    }

    private func rebuildProviderServices(for path: FSPath) {
        if case .local = path.provider {
            rebuildServices(for: URL(fileURLWithPath: path.path))
        } else {
            // Remote: no local index/git watcher.
            folderWatcher = nil
            index = nil
            git = nil
        }
    }

    // rebuildServices(for:URL) remains for local path.
    // existing function unchanged
}
```

- [ ] **Step 4: Callers 조정**

- `ContentView.swift`'s `.onChange(of: tab?.currentFolder)` — gate the `lastFolder.save(url:)` on `.local` provider and use `tab?.currentPath` for provider-agnostic code paths
- `BreadcrumbBar.swift` — accept FSPath, render scheme + segments
- `FileListCoordinator.handleOpen(_:tab:)` — detect provider of `entry.path` and call `tab.navigate(to: FSPath(provider: tab.provider.identifier, path: entry.path.toString()))` instead of `URL(fileURLWithPath:)`
- `CommandPaletteModel` (⌘K) — palette results are local-only in v1; no change needed
- `CairnApp.swift` NavigateCommands "Open Selected" — already keys off selection; fine

Concrete edit for ContentView:

```swift
.onChange(of: tab?.currentPath) { _, new in
    guard let tab else { return }
    guard let path = new else { tab.folder.clear(); return }
    if case .local = path.provider {
        app.lastFolder.save(URL(fileURLWithPath: path.path))
    }
    Task { await tab.folder.load(path, via: tab.provider) }
    triggerSearchRefresh()
}
```

- [ ] **Step 5: 빌드 + 기존 테스트 전수 통과 확인**

```bash
make swift && xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | grep -E "Test Suite|passed|failed" | tail -20
```

Expected: all existing tests green (no regression). The FSPath type change should be mechanical.

- [ ] **Step 6: 수동 smoke — 기존 로컬 기능 확인**

```bash
make run
```

체크: Home 폴더 열기 → 탐색 → Pinned 클릭 → rename (Enter) → delete (⌘⌫) → search (⌘F) → palette (⌘K) → 전부 기존과 동일 동작.

- [ ] **Step 7: Commit**

```bash
git add apps/
git commit -m "refactor(model): FSPath-based Tab + NavigationHistory + FolderModel

Tab now holds a FileSystemProvider and FSPath-based history. Local
tabs use LocalFileSystemProvider (pass-through), so existing behavior
is unchanged — verified by full local test suite + manual smoke.

currentFolder: URL? is kept as a compatibility facade returning nil
for non-local providers; callsites that must have a local URL gate on
it. New navigate(to: FSPath) is the canonical entry. Remote tabs
skip index/git/folderWatcher rebuild since those are local-only.

This sets the seam for SshFileSystemProvider in Task 11."
```

---

## Task 11: `SshFileSystemProvider`

**Files:**
- Create: `apps/Sources/Services/SshFileSystemProvider.swift`
- Create: `apps/CairnTests/SSH/SshFileSystemProviderTests.swift`

- [ ] **Step 1: `SshFileSystemProvider.swift`**

```swift
import Foundation

/// FileSystemProvider backed by a cairn-ssh SftpHandle. The pool + SFTP
/// handle lifecycle is owned by SshPoolService; this type just dispatches
/// operations for a specific (ConnKey, path-root) pair.
final class SshFileSystemProvider: FileSystemProvider {
    let identifier: ProviderID
    let displayScheme: String? = "ssh://"
    let supportsServerSideCopy: Bool

    private let pool: SshPoolService
    private let target: SshTarget

    init(pool: SshPoolService, target: SshTarget, supportsServerSideCopy: Bool) {
        self.pool = pool
        self.target = target
        self.identifier = .ssh(target)
        self.supportsServerSideCopy = supportsServerSideCopy
    }

    private func handle() async throws -> SftpHandleBridge {
        try await pool.sftpHandle(for: target)
    }

    func list(_ path: FSPath) async throws -> [FileEntry] {
        let h = try await handle()
        let entries = try h.list(path.path)
        return entries.map { $0.toFileEntry(parent: path) }
    }

    func stat(_ path: FSPath) async throws -> FileStat {
        let h = try await handle()
        let s = try h.stat(path.path)
        return FileStat(size: Int64(s.size), mtime: Date(timeIntervalSince1970: TimeInterval(s.mtime)),
                        mode: s.mode, isDirectory: s.is_dir)
    }

    func mkdir(_ path: FSPath) async throws {
        let h = try await handle(); try h.mkdir(path.path)
    }

    func rename(from: FSPath, to: FSPath) async throws {
        let h = try await handle(); try h.rename(from.path, to.path)
    }

    func delete(_ paths: [FSPath]) async throws {
        let h = try await handle()
        for p in paths { try h.unlink(p.path) }
    }

    func copyInPlace(from: FSPath, to: FSPath) async throws {
        let h = try await handle()
        if supportsServerSideCopy {
            try h.serverSideCopy(from.path, to.path)
        } else {
            // Client-mediated fallback: download to temp + upload
            let tmp = FileManager.default.temporaryDirectory.appendingPathComponent(UUID().uuidString)
            let cancel = CancelToken()
            try await downloadToLocal(from, toLocalURL: tmp, progress: { _ in }, cancel: cancel)
            try await uploadFromLocal(tmp, to: to, progress: { _ in }, cancel: cancel)
            try? FileManager.default.removeItem(at: tmp)
        }
    }

    func readHead(_ path: FSPath, max: Int) async throws -> Data {
        let h = try await handle()
        let bytes = try h.readHead(path.path, UInt32(max))
        return Data(bytes)
    }

    func downloadToCache(_ path: FSPath) async throws -> URL {
        try await RemoteCacheStore.shared.fetch(remotePath: path, via: self)
    }

    func uploadFromLocal(_ localURL: URL, to remotePath: FSPath, progress: (Int64) -> Void, cancel: CancelToken) async throws {
        let h = try await handle()
        let bridge = CancelFlagBridge.new()
        // Observer task: relay Swift cancel → Rust flag
        let cancelTask = Task {
            while !Task.isCancelled {
                if cancel.isCancelled { bridge.cancel(); break }
                try? await Task.sleep(nanoseconds: 100_000_000)
            }
        }
        defer { cancelTask.cancel() }
        // Progress polling: Rust exposes last-reported bytes; poll every 250ms
        let progressTask = Task { [weak h] in
            while !Task.isCancelled {
                guard let h else { break }
                progress(Int64(h.progressPoll()))
                try? await Task.sleep(nanoseconds: 250_000_000)
            }
        }
        defer { progressTask.cancel() }
        try h.uploadSync(localURL.path, remotePath.path, bridge)
    }

    func downloadToLocal(_ remotePath: FSPath, toLocalURL: URL, progress: (Int64) -> Void, cancel: CancelToken) async throws {
        let h = try await handle()
        let bridge = CancelFlagBridge.new()
        let cancelTask = Task {
            while !Task.isCancelled {
                if cancel.isCancelled { bridge.cancel(); break }
                try? await Task.sleep(nanoseconds: 100_000_000)
            }
        }
        defer { cancelTask.cancel() }
        let progressTask = Task { [weak h] in
            while !Task.isCancelled {
                guard let h else { break }
                progress(Int64(h.progressPoll()))
                try? await Task.sleep(nanoseconds: 250_000_000)
            }
        }
        defer { progressTask.cancel() }
        try h.downloadSync(remotePath.path, toLocalURL.path, bridge)
    }
}

private extension FileEntryBridge {
    func toFileEntry(parent: FSPath) -> FileEntry {
        // FileEntry is defined by the existing Rust walker bridge. Here we
        // construct an equivalent for remote listings — name, kind (Directory
        // | Regular), size, mtime, is_hidden (. prefix). The exact type
        // shape follows apps/Sources/Generated/cairn_ffi.swift FileEntry.
        let fullPath = (parent.path.hasSuffix("/") ? parent.path : parent.path + "/") + name
        return FileEntry(
            name: RustString(name),
            path: RustString(fullPath),
            kind: is_dir ? .Directory : .Regular,
            size: size,
            modified_unix: UInt64(mtime),
            is_hidden: name.hasPrefix(".")
        )
    }
}
```

> **Implementer note:** `FileEntry` construction must match the exact shape in `apps/Sources/Generated/cairn_ffi.swift`. If the Rust walker's `FileEntry` type has additional fields (e.g., uid/gid, symlink flag), mirror those with sensible defaults here.

- [ ] **Step 2: `SshFileSystemProviderTests.swift` — FFI 타입 round-trip**

```swift
import XCTest
@testable import Cairn

final class SshFileSystemProviderTests: XCTestCase {
    /// Smoke: provider construction doesn't crash and reports ssh:// scheme.
    func testProviderIdentity() {
        let target = SshTarget(user: "deploy", hostname: "prod-api", port: 22, configHashHex: "0".padding(toLength: 32, withPad: "0", startingAt: 0))
        let pool = SshPoolService.forTesting()
        let provider = SshFileSystemProvider(pool: pool, target: target, supportsServerSideCopy: false)
        XCTAssertEqual(provider.displayScheme, "ssh://")
        XCTAssertFalse(provider.supportsServerSideCopy)
        if case .ssh(let t) = provider.identifier {
            XCTAssertEqual(t.hostname, "prod-api")
        } else {
            XCTFail("expected .ssh identifier")
        }
    }
}
```

> Note: `SshPoolService.forTesting()` is a factory introduced in Task 12. Defer this test until Task 12 lands OR stub the factory here and fill in then.

- [ ] **Step 3: 빌드 확인** (테스트는 Task 12 이후)

```bash
make swift 2>&1 | tail -10
```

Expected: BUILD SUCCEEDED.

- [ ] **Step 4: Commit**

```bash
git add apps/
git commit -m "feat(providers): SshFileSystemProvider bridges SftpHandle to Swift

Dispatches list/stat/mkdir/rename/delete/readHead/copyInPlace through
the shared SshPoolService which owns the pool + per-session SFTP
handles. Progress reporting uses a poll loop at 250ms intervals
reading the Rust-side AtomicU64 — callback-from-Rust for progress is
deferred to M2 to keep the v1 FFI surface synchronous.

copyInPlace prefers server-side copy-data extension when the server
supports it; falls back to client-mediated download→upload via a temp
file. RemoteCacheStore (Task 23) supplies downloadToCache."
```

---

## Task 12: `SshPoolService` + resolvers + Keychain

**Files:**
- Create: `apps/Sources/Services/SshPoolService.swift`
- Create: `apps/Sources/Services/HostKeyAlertResolver.swift`
- Create: `apps/Sources/Services/PassphraseResolver.swift`
- Create: `apps/Sources/Services/KeychainPassphraseStore.swift`
- Modify: `apps/Sources/App/AppModel.swift` (add `ssh: SshPoolService` + graceful shutdown)

- [ ] **Step 1: `KeychainPassphraseStore.swift`**

```swift
import Foundation
import Security

/// Reads/writes SSH key passphrases to macOS Keychain.
/// Account format: "ssh-key:<absolute path>"; service: "com.cairn.ssh.passphrase".
enum KeychainPassphraseStore {
    private static let service = "com.cairn.ssh.passphrase"

    static func load(for keyPath: String) -> String? {
        let query: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: "ssh-key:\(keyPath)",
            kSecReturnData as String: true,
            kSecMatchLimit as String: kSecMatchLimitOne,
        ]
        var item: CFTypeRef?
        let status = SecItemCopyMatching(query as CFDictionary, &item)
        guard status == errSecSuccess, let data = item as? Data else { return nil }
        return String(data: data, encoding: .utf8)
    }

    static func save(_ passphrase: String, for keyPath: String) {
        let account = "ssh-key:\(keyPath)"
        let delete: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: account,
        ]
        SecItemDelete(delete as CFDictionary)
        let add: [String: Any] = [
            kSecClass as String: kSecClassGenericPassword,
            kSecAttrService as String: service,
            kSecAttrAccount as String: account,
            kSecValueData as String: Data(passphrase.utf8),
            kSecAttrAccessible as String: kSecAttrAccessibleWhenUnlocked,
        ]
        SecItemAdd(add as CFDictionary, nil)
    }
}
```

- [ ] **Step 2: `PassphraseResolver.swift` — actor + alert**

```swift
import AppKit

/// Serializes passphrase prompts so concurrent connects don't stack modals.
/// Checks Keychain first (hit → skip prompt).
actor PassphraseResolver {
    func resolve(keyPath: String) async -> String? {
        if let cached = KeychainPassphraseStore.load(for: keyPath) { return cached }
        return await MainActor.run { presentAlert(keyPath: keyPath) }
    }

    @MainActor
    private func presentAlert(keyPath: String) -> String? {
        let alert = NSAlert()
        alert.messageText = "Unlock \((keyPath as NSString).abbreviatingWithTildeInPath)"
        alert.informativeText = "Enter the passphrase for this SSH key."
        alert.addButton(withTitle: "Unlock")
        alert.addButton(withTitle: "Cancel")

        let field = NSSecureTextField(frame: NSRect(x: 0, y: 0, width: 240, height: 22))
        let check = NSButton(checkboxWithTitle: "Remember in Keychain", target: nil, action: nil)
        check.state = .on

        let stack = NSStackView(views: [field, check])
        stack.orientation = .vertical
        stack.alignment = .leading
        stack.spacing = 8
        stack.frame = NSRect(x: 0, y: 0, width: 260, height: 52)
        alert.accessoryView = stack

        guard alert.runModal() == .alertFirstButtonReturn else { return nil }
        let pass = field.stringValue
        if check.state == .on, !pass.isEmpty {
            KeychainPassphraseStore.save(pass, for: keyPath)
        }
        return pass.isEmpty ? nil : pass
    }
}
```

- [ ] **Step 3: `HostKeyAlertResolver.swift`**

```swift
import AppKit

actor HostKeyAlertResolver {
    func resolve(host: String, port: UInt16, offer: HostKeyOffer, knownState: String) async -> String {
        await MainActor.run { presentAlert(host: host, port: port, offer: offer, knownState: knownState) }
    }

    @MainActor
    private func presentAlert(host: String, port: UInt16, offer: HostKeyOffer, knownState: String) -> String {
        let alert = NSAlert()
        switch knownState {
        case "mismatch":
            alert.alertStyle = .critical
            alert.messageText = "⚠︎ Host key CHANGED for \"\(host)\""
            alert.informativeText = "Offered: \(offer.fingerprint)\n\nPossible MITM attack, or the host was reinstalled. Remove the old key in terminal: ssh-keygen -R \(host)"
            alert.addButton(withTitle: "Cancel")
            _ = alert.runModal()
            return "reject"
        case "not_found":
            alert.alertStyle = .warning
            alert.messageText = "New host key for \"\(host)\""
            alert.informativeText = "\(offer.fingerprint)\nAlgorithm: \(offer.algorithm)\n\nFirst connection to this host. Verify the fingerprint matches the server."
            alert.addButton(withTitle: "Accept & Save")
            alert.addButton(withTitle: "Accept Once")
            alert.addButton(withTitle: "Cancel")
            switch alert.runModal() {
            case .alertFirstButtonReturn:  return "accept_save"
            case .alertSecondButtonReturn: return "accept"
            default:                       return "reject"
            }
        default:
            return "accept"
        }
    }
}
```

- [ ] **Step 4: `SshPoolService.swift`**

```swift
import Foundation
import Observation

@Observable
final class SshPoolService {
    private let pool: SshPoolBridge
    private let hostKeyResolver: HostKeyAlertResolver
    private let passphraseResolver: PassphraseResolver

    private(set) var sessions: [SshTarget: SessionState] = [:]

    struct SessionState {
        enum Status { case connecting, active, idle, error(String) }
        var status: Status
        var lastActivity: Date
        var resolvedSummary: String          // tooltip: "user@host:port · via cloudflared"
    }

    private var sftpHandles: [SshTarget: SftpHandleBridge] = [:]
    private var reaperTimer: Timer?

    init() {
        self.pool = SshPoolBridge.new()
        self.hostKeyResolver = HostKeyAlertResolver()
        self.passphraseResolver = PassphraseResolver()
        startReaperTimer()
    }

    static func forTesting() -> SshPoolService { SshPoolService() }

    func startReaperTimer() {
        reaperTimer?.invalidate()
        reaperTimer = Timer.scheduledTimer(withTimeInterval: 60, repeats: true) { [weak self] _ in
            self?.refreshSessionStates()
        }
    }

    func connect(hostAlias: String, overrides: ConnectSpecOverrides = .init()) async throws -> SshTarget {
        // TODO: wire hostKeyCallback / passphraseCallback through FFI; see Task 8
        // implementer note. For now, call pool.connect synchronously on a
        // background task.
        let spec = ConnectSpecBridge(
            host_alias: hostAlias,
            user_override: overrides.user,
            port_override: overrides.port,
            identity_file_override: overrides.identityFile,
            proxy_command_override: overrides.proxyCommand
        )
        let result = await Task.detached(priority: .userInitiated) { [pool] in
            pool.connect(spec)
        }.value
        let key = try result.get()
        let target = SshTarget(user: key.user, hostname: key.hostname, port: key.port, configHashHex: key.config_hash_hex)
        sessions[target] = SessionState(status: .active, lastActivity: Date(), resolvedSummary: "\(key.user)@\(key.hostname):\(key.port)")
        return target
    }

    func sftpHandle(for target: SshTarget) async throws -> SftpHandleBridge {
        if let h = sftpHandles[target] { return h }
        let key = ConnKeyBridge(user: target.user, hostname: target.hostname, port: target.port, config_hash_hex: target.configHashHex)
        let h = try pool.openSftp(key).get()
        sftpHandles[target] = h
        return h
    }

    func disconnect(_ target: SshTarget) {
        sftpHandles.removeValue(forKey: target)
        let key = ConnKeyBridge(user: target.user, hostname: target.hostname, port: target.port, config_hash_hex: target.configHashHex)
        pool.disconnect(key)
        sessions.removeValue(forKey: target)
    }

    func closeAll() {
        sftpHandles.removeAll()
        pool.closeAll()
        sessions.removeAll()
    }

    private func refreshSessionStates() {
        // Poll Rust pool for live keys and reconcile.
        let live = pool.snapshotKeys()
        let liveTargets = Set(live.map { SshTarget(user: $0.user, hostname: $0.hostname, port: $0.port, configHashHex: $0.config_hash_hex) })
        for t in sessions.keys where !liveTargets.contains(t) {
            sessions[t]?.status = .idle
            sftpHandles.removeValue(forKey: t)
        }
    }
}

struct ConnectSpecOverrides {
    var user: String?
    var port: UInt16?
    var identityFile: String?
    var proxyCommand: String?
}
```

- [ ] **Step 5: `AppModel` 통합 + 앱 종료 훅**

Modify `AppModel.swift`:

```swift
@Observable
final class AppModel {
    // … existing
    let ssh: SshPoolService
    let transfers: TransferController          // Task 20 places this

    init(...) {
        // … existing
        self.ssh = SshPoolService()
        self.transfers = TransferController()
        // Graceful quit
        NotificationCenter.default.addObserver(
            forName: NSApplication.willTerminateNotification,
            object: nil, queue: .main
        ) { [ssh] _ in ssh.closeAll() }
    }
}
```

- [ ] **Step 6: 빌드**

```bash
make swift 2>&1 | tail -10
```

Expected: BUILD SUCCEEDED. HostKey/Passphrase callbacks는 아직 FFI에 완전 wired되지 않았지만 스켈레톤 컴파일은 OK.

- [ ] **Step 7: Commit**

```bash
git add apps/
git commit -m "feat(ssh): SshPoolService + host-key + passphrase resolvers

Wraps SshPoolBridge, tracks per-target SessionState (status +
lastActivity + tooltip summary), memoizes SftpHandle per target so
provider calls don't re-open sessions.

HostKeyAlertResolver serializes TOFU and changed-key prompts via an
actor + @MainActor NSAlert. Changed-key alert is critical-styled with
no Accept path.

PassphraseResolver checks Keychain first
(com.cairn.ssh.passphrase/ssh-key:<path>); falls back to an NSAlert
with NSSecureTextField + 'Remember in Keychain' checkbox. Saved
passphrases use kSecAttrAccessibleWhenUnlocked.

AppModel wires ssh.closeAll() to NSApplication.willTerminate for
graceful quit. HostKey/Passphrase FFI plumbing is still deferred to
the FFI callback pass — see Task 8 implementer note."
```

---

## Task 13: `HostMetadataStore` sidecar + `ConfigFileWatcher`

**Files:**
- Create: `apps/Sources/Services/HostMetadataStore.swift`
- Create: `apps/Sources/Services/ConfigFileWatcher.swift`
- Create: `apps/CairnTests/SSH/HostMetadataStoreTests.swift`

- [ ] **Step 1: 기존 컨벤션 조사**

```bash
grep -rn "Application Support\|applicationSupportDirectory\|NSApplicationSupport" apps/Sources/Services/BookmarkStore.swift apps/Sources/Services/SettingsStore.swift 2>&1 | head -20
```

목표: sidecar JSON 위치를 `BookmarkStore`와 맞추기. 보통 `FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask).first.appendingPathComponent("Cairn")` 패턴.

- [ ] **Step 2: `HostMetadataStore.swift`**

```swift
import Foundation

struct HostMetadata: Codable, Hashable {
    var lastConnectedAt: Date?
    var pinned: Bool
    var hiddenFromSidebar: Bool
    var lastKnownState: String?          // "ok" | "error" | nil
}

final class HostMetadataStore {
    private let fileURL: URL
    private let queue = DispatchQueue(label: "com.cairn.ssh.host-metadata")
    private var cache: [String: HostMetadata] = [:]

    init(url: URL? = nil) {
        if let url {
            self.fileURL = url
        } else {
            let support = FileManager.default.urls(for: .applicationSupportDirectory, in: .userDomainMask).first!
            let dir = support.appendingPathComponent("Cairn", isDirectory: true)
            try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
            self.fileURL = dir.appendingPathComponent("host-metadata.json")
        }
        load()
    }

    func metadata(for host: String) -> HostMetadata {
        queue.sync { cache[host] ?? HostMetadata(lastConnectedAt: nil, pinned: false, hiddenFromSidebar: false, lastKnownState: nil) }
    }

    func update(_ host: String, _ mutate: (inout HostMetadata) -> Void) {
        queue.sync {
            var m = cache[host] ?? HostMetadata(lastConnectedAt: nil, pinned: false, hiddenFromSidebar: false, lastKnownState: nil)
            mutate(&m)
            cache[host] = m
            persistLocked()
        }
    }

    func all() -> [String: HostMetadata] { queue.sync { cache } }

    private func load() {
        queue.sync {
            guard let data = try? Data(contentsOf: fileURL) else { return }
            cache = (try? JSONDecoder().decode([String: HostMetadata].self, from: data)) ?? [:]
        }
    }

    private func persistLocked() {
        let enc = JSONEncoder()
        enc.outputFormatting = [.prettyPrinted, .sortedKeys]
        enc.dateEncodingStrategy = .iso8601
        guard let data = try? enc.encode(cache) else { return }
        try? data.write(to: fileURL, options: .atomic)
    }
}
```

- [ ] **Step 3: `ConfigFileWatcher.swift` — FSEvents on ssh_config tree**

```swift
import Foundation

/// Watches ~/.ssh/config (+ first-level ~/.ssh/config.d/* glob) for changes
/// and fires a callback so the sidebar can reload its host list.
final class ConfigFileWatcher {
    private let callback: () -> Void
    private var stream: FSEventStreamRef?

    init(callback: @escaping () -> Void) {
        self.callback = callback
        start()
    }

    deinit { stop() }

    private func start() {
        guard let home = ProcessInfo.processInfo.environment["HOME"] else { return }
        let paths: [String] = [
            "\(home)/.ssh/config",
            "\(home)/.ssh/config.d",
        ]
        // FSEvents wants directories; if config file is watched, use its parent.
        let watchDirs = Array(Set(paths.map { ($0 as NSString).deletingLastPathComponent }))
        let pathsCF = watchDirs as CFArray
        var context = FSEventStreamContext(version: 0, info: Unmanaged.passUnretained(self).toOpaque(), retain: nil, release: nil, copyDescription: nil)
        let cb: FSEventStreamCallback = { _, info, _, _, _, _ in
            guard let info else { return }
            let w = Unmanaged<ConfigFileWatcher>.fromOpaque(info).takeUnretainedValue()
            w.callback()
        }
        stream = FSEventStreamCreate(
            nil, cb, &context, pathsCF,
            FSEventStreamEventId(kFSEventStreamEventIdSinceNow),
            0.5, FSEventStreamCreateFlags(kFSEventStreamCreateFlagFileEvents)
        )
        if let s = stream {
            FSEventStreamSetDispatchQueue(s, DispatchQueue.main)
            FSEventStreamStart(s)
        }
    }

    private func stop() {
        if let s = stream {
            FSEventStreamStop(s)
            FSEventStreamInvalidate(s)
            FSEventStreamRelease(s)
        }
        stream = nil
    }
}
```

- [ ] **Step 4: `HostMetadataStoreTests.swift`**

```swift
import XCTest
@testable import Cairn

final class HostMetadataStoreTests: XCTestCase {
    func testRoundTrip() {
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent("\(UUID()).json")
        defer { try? FileManager.default.removeItem(at: tmp) }
        let store = HostMetadataStore(url: tmp)
        store.update("prod-api") { $0.pinned = true; $0.lastConnectedAt = Date(timeIntervalSince1970: 0) }
        let reloaded = HostMetadataStore(url: tmp)
        XCTAssertTrue(reloaded.metadata(for: "prod-api").pinned)
    }

    func testDefaultEmpty() {
        let store = HostMetadataStore(url: FileManager.default.temporaryDirectory.appendingPathComponent("\(UUID()).json"))
        XCTAssertFalse(store.metadata(for: "unknown").pinned)
        XCTAssertNil(store.metadata(for: "unknown").lastConnectedAt)
    }
}
```

- [ ] **Step 5: 빌드 + 테스트**

```bash
make swift && xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | grep -E "HostMetadata" | tail -5
```

Expected: HostMetadataStoreTests 2 passed.

- [ ] **Step 6: Commit**

```bash
git add apps/
git commit -m "feat(ssh): HostMetadataStore sidecar + ssh_config FSEventsWatcher

HostMetadataStore persists per-host Cairn-side metadata (pinned,
hiddenFromSidebar, lastConnectedAt, lastKnownState) as JSON at
~/Library/Application Support/Cairn/host-metadata.json — location
matches BookmarkStore/SettingsStore convention. ssh_config stays
untouched.

ConfigFileWatcher tracks ~/.ssh/config + ~/.ssh/config.d/ via
FSEventStream. Callback fires on create/modify/delete — the sidebar
re-reads list_configured_hosts() and diff-updates its section."
```

---

## Task 14: `SshConfigWriter` + `SshConfigService`

**Files:**
- Create: `apps/Sources/Services/SshConfigWriter.swift`
- Create: `apps/Sources/Services/SshConfigService.swift`
- Create: `apps/CairnTests/SSH/SshConfigWriterTests.swift`

- [ ] **Step 1: `SshConfigWriter.swift` — append-only writer**

```swift
import Foundation

/// Appends a new Host block to ~/.ssh/config while preserving existing
/// file contents, trailing newline, and 0600 permissions. Never edits
/// or removes existing entries — v1 is append-only.
enum SshConfigWriter {
    struct Entry {
        var nickname: String
        var hostname: String?
        var port: UInt16?
        var user: String?
        var identityFile: String?
        var proxyCommand: String?
    }

    enum WriterError: Error { case invalidNickname, write(Error) }

    static func append(_ entry: Entry, to configURL: URL) throws {
        let trimmed = entry.nickname.trimmingCharacters(in: .whitespaces)
        guard !trimmed.isEmpty, !trimmed.contains(where: { " \t\n".contains($0) }) else {
            throw WriterError.invalidNickname
        }

        // Read existing contents (or empty) — must preserve CRLF/LF style.
        let existing: String
        if FileManager.default.fileExists(atPath: configURL.path) {
            existing = (try? String(contentsOf: configURL, encoding: .utf8)) ?? ""
        } else {
            if let dir = configURL.deletingLastPathComponent().path as String? {
                try? FileManager.default.createDirectory(atPath: dir, withIntermediateDirectories: true, attributes: [.posixPermissions: 0o700])
            }
            existing = ""
        }

        var block = "\nHost \(trimmed)\n"
        if let h = entry.hostname { block += "    HostName \(h)\n" }
        if let p = entry.port, p != 22 { block += "    Port \(p)\n" }
        if let u = entry.user { block += "    User \(u)\n" }
        if let k = entry.identityFile { block += "    IdentityFile \(k)\n" }
        if let pc = entry.proxyCommand { block += "    ProxyCommand \(pc)\n" }

        let combined = existing + block

        do {
            try combined.write(to: configURL, atomically: true, encoding: .utf8)
            // Preserve / enforce 0600 permissions
            try FileManager.default.setAttributes([.posixPermissions: 0o600], ofItemAtPath: configURL.path)
        } catch {
            throw WriterError.write(error)
        }
    }
}
```

- [ ] **Step 2: `SshConfigService.swift` — facade**

```swift
import Foundation

@Observable
final class SshConfigService {
    private(set) var configuredHosts: [String] = []
    private let metadata: HostMetadataStore
    private var watcher: ConfigFileWatcher?

    init(metadata: HostMetadataStore) {
        self.metadata = metadata
        reload()
        self.watcher = ConfigFileWatcher { [weak self] in
            Task { @MainActor in self?.reload() }
        }
    }

    func reload() {
        configuredHosts = SshPoolBridge.listConfiguredHosts()   // Task 8 FFI
    }

    func appendHost(_ entry: SshConfigWriter.Entry) throws {
        let home = ProcessInfo.processInfo.environment["HOME"]!
        let url = URL(fileURLWithPath: "\(home)/.ssh/config")
        try SshConfigWriter.append(entry, to: url)
        reload()
    }

    func metadataFor(_ host: String) -> HostMetadata { metadata.metadata(for: host) }

    func touch(_ host: String, state: String?) {
        metadata.update(host) {
            $0.lastConnectedAt = Date()
            if let state { $0.lastKnownState = state }
        }
    }
}
```

- [ ] **Step 3: `SshConfigWriterTests.swift`**

```swift
import XCTest
@testable import Cairn

final class SshConfigWriterTests: XCTestCase {
    func testAppendCreatesBlock() throws {
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent("\(UUID()).config")
        defer { try? FileManager.default.removeItem(at: tmp) }
        try SshConfigWriter.append(.init(
            nickname: "prod-api",
            hostname: "10.0.1.5",
            port: nil, user: "deploy",
            identityFile: "~/.ssh/id_ed25519",
            proxyCommand: nil
        ), to: tmp)
        let s = try String(contentsOf: tmp, encoding: .utf8)
        XCTAssertTrue(s.contains("Host prod-api"))
        XCTAssertTrue(s.contains("HostName 10.0.1.5"))
        XCTAssertTrue(s.contains("User deploy"))
        XCTAssertFalse(s.contains("Port "))   // default port omitted
    }

    func testPreservesExistingContent() throws {
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent("\(UUID()).config")
        defer { try? FileManager.default.removeItem(at: tmp) }
        try "Host staging\n    HostName 10.0.0.5\n".write(to: tmp, atomically: true, encoding: .utf8)
        try SshConfigWriter.append(.init(nickname: "prod", hostname: "1.2.3.4", port: nil, user: nil, identityFile: nil, proxyCommand: nil), to: tmp)
        let s = try String(contentsOf: tmp, encoding: .utf8)
        XCTAssertTrue(s.contains("Host staging"))
        XCTAssertTrue(s.contains("Host prod"))
    }

    func testRejectsInvalidNickname() {
        let tmp = FileManager.default.temporaryDirectory.appendingPathComponent("\(UUID()).config")
        XCTAssertThrowsError(try SshConfigWriter.append(.init(nickname: "has space", hostname: "x", port: nil, user: nil, identityFile: nil, proxyCommand: nil), to: tmp))
    }
}
```

- [ ] **Step 4: 빌드 + 테스트**

```bash
make swift && xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | grep SshConfig | tail -10
```

Expected: 3 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/
git commit -m "feat(ssh): append-only ssh_config writer + SshConfigService facade

SshConfigWriter.append preserves existing file bytes, adds a blank
line separator, emits indented subdirectives, and restores 0600
permissions atomically. Invalid nicknames (empty / whitespace) throw
before touching disk.

SshConfigService is the @Observable facade wrapping
list_configured_hosts (FFI) + ConfigFileWatcher + HostMetadataStore.
Sidebar binds to configuredHosts; mutations go through appendHost
(which calls reload so the UI updates before FSEvents fires)."
```

---

## Task 15: 사이드바 "Remote Hosts" 섹션 + `RemoteHostRow`

**Files:**
- Create: `apps/Sources/Views/Sidebar/RemoteHostRow.swift`
- Modify: `apps/Sources/ViewModels/SidebarModel.swift`
- Modify: `apps/Sources/Views/Sidebar/SidebarView.swift`

- [ ] **Step 1: `SidebarModel.swift` 확장**

Add a hosts section:

```swift
extension SidebarModel {
    struct RemoteHostItem: Identifiable, Hashable {
        let id: String              // host nickname
        let displaySummary: String  // "user@host:port · via cloudflared" or ""
        let state: State
        let pinned: Bool
        enum State: String { case connected, idle, error, disconnected }
    }

    func remoteHostItems(from svc: SshConfigService, pool: SshPoolService) -> [RemoteHostItem] {
        svc.configuredHosts.compactMap { name in
            let meta = svc.metadataFor(name)
            if meta.hiddenFromSidebar { return nil }
            // Map to SshTarget by looking up session with matching hostname/alias — pool keys by resolved user/host/port/hash, so we match by nickname via a helper on the pool.
            let state: RemoteHostItem.State
            if pool.sessions.values.contains(where: { $0.resolvedSummary.contains(name) }) {
                state = .connected
            } else {
                state = .disconnected
            }
            return RemoteHostItem(
                id: name,
                displaySummary: meta.lastConnectedAt.map { "last connected \(relative($0))" } ?? "",
                state: state,
                pinned: meta.pinned
            )
        }.sorted { ($0.pinned, svc.metadataFor($0.id).lastConnectedAt ?? .distantPast) >
                   ($1.pinned, svc.metadataFor($1.id).lastConnectedAt ?? .distantPast) }
    }

    private func relative(_ d: Date) -> String {
        let f = RelativeDateTimeFormatter()
        f.unitsStyle = .abbreviated
        return f.localizedString(for: d, relativeTo: Date())
    }
}
```

- [ ] **Step 2: `RemoteHostRow.swift`**

```swift
import SwiftUI

struct RemoteHostRow: View {
    let item: SidebarModel.RemoteHostItem
    var onConnect: () -> Void
    var onDisconnect: () -> Void
    var onHide: () -> Void
    var onRevealConfig: () -> Void
    var onCopySshCommand: () -> Void

    var body: some View {
        HStack(spacing: 8) {
            Circle()
                .fill(dotColor)
                .frame(width: 7, height: 7)
                .shadow(color: dotColor.opacity(0.6), radius: dotGlow, y: 0)
            Text(item.id).lineLimit(1).truncationMode(.middle)
            if item.pinned {
                Image(systemName: "pin.fill").font(.system(size: 9)).foregroundStyle(.secondary)
            }
            Spacer()
        }
        .contentShape(Rectangle())
        .onTapGesture { onConnect() }
        .help(item.displaySummary)
        .contextMenu {
            Button("Connect") { onConnect() }
            Button("Disconnect") { onDisconnect() }
                .disabled(item.state != .connected)
            Divider()
            Button("Copy ssh Command") { onCopySshCommand() }
            Button("Reveal ssh_config Entry") { onRevealConfig() }
            Divider()
            Button("Hide from Sidebar") { onHide() }
        }
    }

    private var dotColor: Color {
        switch item.state {
        case .connected:    return Color(red: 0.42, green: 0.85, blue: 0.61)
        case .idle:         return Color(red: 0.85, green: 0.77, blue: 0.41)
        case .error:        return Color(red: 1.00, green: 0.53, blue: 0.53)
        case .disconnected: return Color(red: 0.33, green: 0.36, blue: 0.42)
        }
    }
    private var dotGlow: CGFloat { item.state == .connected ? 4 : 0 }
}
```

- [ ] **Step 3: `SidebarView.swift` — 섹션 추가**

Append the new section under existing Pinned/Home/Recent sections (before GitBranchFooter):

```swift
// In SidebarView body, after existing sections
Section(header: Text("Remote Hosts").font(.system(size: 11)).foregroundStyle(.secondary)) {
    ForEach(remoteHosts, id: \.id) { item in
        RemoteHostRow(
            item: item,
            onConnect: { connectHost(item.id) },
            onDisconnect: { disconnectHost(item.id) },
            onHide: { hideHost(item.id) },
            onRevealConfig: { revealConfig() },
            onCopySshCommand: { copySshCommand(item.id) }
        )
    }
    Button {
        openConnectSheet()
    } label: {
        Label("Connect…", systemImage: "plus.circle")
            .foregroundStyle(.secondary)
    }
    .buttonStyle(.plain)
}
```

Supporting helpers (bind to `app.ssh`, `app.sshConfig`, `scene.newRemoteTab(...)` — Task 16 wires the sheet and Task 18 wires the tab).

- [ ] **Step 4: 빌드 + 수동 확인**

```bash
make build && make run
```

체크: ssh_config에 최소 1개 non-wildcard Host 엔트리 추가 → 사이드바 "Remote Hosts" 섹션에 나오는지. "+ Connect…" 버튼 보이는지. 색점 색상 맞는지.

- [ ] **Step 5: Commit**

```bash
git add apps/
git commit -m "feat(sidebar): Remote Hosts section sourced from ssh_config

New section under Pinned/Home lists non-wildcard Host blocks from
~/.ssh/config (+ Include). Each row shows a state dot (connected /
idle / error / disconnected), nickname, and a pin indicator; hover
tooltip shows last-connected relative time. Click → connect (new
tab). Context menu: Connect / Disconnect / Copy ssh / Reveal config /
Hide.

Sort: pinned first, then lastConnectedAt desc. Hidden hosts are
filtered via HostMetadataStore's hiddenFromSidebar flag."
```

---

## Task 16: Connect sheet

**Files:**
- Create: `apps/Sources/Views/Connect/ConnectSheetModel.swift`
- Create: `apps/Sources/Views/Connect/ConnectSheetView.swift`
- Modify: `apps/Sources/CairnApp.swift` (menu item ⇧⌘K + File menu)
- Modify: `apps/Sources/ContentView.swift` (sheet presentation)

- [ ] **Step 1: `ConnectSheetModel.swift`**

```swift
import Foundation
import Observation

@Observable
final class ConnectSheetModel {
    var server: String = ""              // "user@host" or "host"
    var port: String = "22"
    var path: String = "~"
    enum AuthMode: String { case agent, keyFile }
    var authMode: AuthMode = .agent
    var keyFile: String = "~/.ssh/id_ed25519"
    var proxyCommand: String = ""
    var showAdvanced: Bool = false
    var saveToConfig: Bool = false
    var nickname: String = ""
    var connecting: Bool = false
    var error: String? = nil

    /// Parse `server` into user + host.
    func resolveUserHost() -> (user: String?, host: String) {
        if let at = server.firstIndex(of: "@") {
            return (String(server[server.startIndex..<at]), String(server[server.index(after: at)...]))
        } else {
            return (nil, server)
        }
    }

    /// Parse `ssh://user@host:port/path` clipboard into fields.
    func acceptURL(_ url: String) {
        guard let parsed = URLComponents(string: url), parsed.scheme == "ssh" else { return }
        if let u = parsed.user, let h = parsed.host {
            server = "\(u)@\(h)"
        } else if let h = parsed.host {
            server = h
        }
        if let p = parsed.port { port = String(p) }
        if !parsed.path.isEmpty { path = parsed.path }
    }
}
```

- [ ] **Step 2: `ConnectSheetView.swift`**

```swift
import SwiftUI

struct ConnectSheetView: View {
    @Bindable var model: ConnectSheetModel
    var onConnect: () -> Void
    var onCancel: () -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 12) {
            Text("Connect to Server").font(.headline)
            Divider()

            LabeledContent("Server") {
                HStack {
                    TextField("deploy@prod-api", text: $model.server)
                    Text(":")
                    TextField("22", text: $model.port).frame(width: 48)
                }
            }
            LabeledContent("Path") {
                TextField("/var/log/nginx", text: $model.path)
            }

            GroupBox("Auth") {
                VStack(alignment: .leading) {
                    Picker("", selection: $model.authMode) {
                        Text("Agent").tag(ConnectSheetModel.AuthMode.agent)
                        Text("Key file").tag(ConnectSheetModel.AuthMode.keyFile)
                    }.pickerStyle(.radioGroup)
                    if model.authMode == .keyFile {
                        HStack {
                            TextField("~/.ssh/id_ed25519", text: $model.keyFile)
                            Button("Browse…") { pickKeyFile() }
                        }
                    }
                }
            }

            DisclosureGroup("Advanced", isExpanded: $model.showAdvanced) {
                TextField("Custom ProxyCommand", text: $model.proxyCommand)
            }

            Toggle("Save to ~/.ssh/config as:", isOn: $model.saveToConfig)
            if model.saveToConfig {
                TextField("prod-api", text: $model.nickname)
            }

            if let err = model.error {
                Text(err).foregroundStyle(.red).font(.caption)
            }

            HStack {
                Spacer()
                Button("Cancel", action: onCancel).keyboardShortcut(.cancelAction)
                Button("Connect", action: onConnect)
                    .keyboardShortcut(.defaultAction)
                    .disabled(model.server.isEmpty || model.connecting)
            }
        }
        .padding(20)
        .frame(width: 420)
    }

    private func pickKeyFile() {
        let panel = NSOpenPanel()
        panel.canChooseFiles = true
        panel.canChooseDirectories = false
        panel.allowsMultipleSelection = false
        if let home = ProcessInfo.processInfo.environment["HOME"] {
            panel.directoryURL = URL(fileURLWithPath: "\(home)/.ssh")
        }
        if panel.runModal() == .OK, let url = panel.url {
            model.keyFile = url.path
        }
    }
}
```

- [ ] **Step 3: 메뉴 + 시트 presentation**

In `CairnApp.swift`, add a `ConnectCommands` CommandMenu item for `⇧⌘K`:

```swift
Button("Connect to Server\u{2026}") {
    // Publish a @FocusedValue signal that ContentView picks up
    NotificationCenter.default.post(name: .openConnectSheet, object: nil)
}
.keyboardShortcut("k", modifiers: [.command, .shift])
```

Define the notification in a shared place:

```swift
extension Notification.Name {
    static let openConnectSheet = Notification.Name("com.cairn.openConnectSheet")
}
```

In `ContentView.swift`, add state + sheet:

```swift
@State private var connectSheetModel: ConnectSheetModel? = nil

// In body, attach:
.sheet(item: Binding(
    get: { connectSheetModel },
    set: { connectSheetModel = $0 }
)) { model in
    ConnectSheetView(
        model: model,
        onConnect: { performConnect(model: model) },
        onCancel: { connectSheetModel = nil }
    )
}
.onReceive(NotificationCenter.default.publisher(for: .openConnectSheet)) { _ in
    connectSheetModel = ConnectSheetModel()
}
```

`performConnect` (new helper on ContentView):

```swift
private func performConnect(model: ConnectSheetModel) {
    model.connecting = true
    model.error = nil
    Task {
        defer { model.connecting = false }
        do {
            let (user, host) = model.resolveUserHost()

            if model.saveToConfig, !model.nickname.isEmpty {
                try app.sshConfig.appendHost(.init(
                    nickname: model.nickname,
                    hostname: host,
                    port: UInt16(model.port).flatMap { $0 == 22 ? nil : $0 },
                    user: user,
                    identityFile: model.authMode == .keyFile ? model.keyFile : nil,
                    proxyCommand: model.showAdvanced && !model.proxyCommand.isEmpty ? model.proxyCommand : nil
                ))
            }

            let alias = model.saveToConfig && !model.nickname.isEmpty ? model.nickname : host
            let overrides = ConnectSpecOverrides(
                user: user, port: UInt16(model.port),
                identityFile: model.authMode == .keyFile ? model.keyFile : nil,
                proxyCommand: model.showAdvanced && !model.proxyCommand.isEmpty ? model.proxyCommand : nil
            )
            let target = try await app.ssh.connect(hostAlias: alias, overrides: overrides)
            let provider = SshFileSystemProvider(pool: app.ssh, target: target, supportsServerSideCopy: false)
            let initial = FSPath(provider: .ssh(target), path: model.path.isEmpty ? "/" : model.path)
            scene.newRemoteTab(initialPath: initial, provider: provider)
            connectSheetModel = nil
        } catch {
            model.error = (error as NSError).localizedDescription
        }
    }
}
```

`scene.newRemoteTab` is added to `WindowSceneModel` in this same task:

```swift
// WindowSceneModel.swift
func newRemoteTab(initialPath: FSPath, provider: FileSystemProvider) {
    let t = Tab(engine: engine, bookmarks: bookmarks, initialPath: initialPath, provider: provider)
    tabs.append(t)
    activeTabID = t.id
}
```

- [ ] **Step 4: 빌드 + 수동 확인**

```bash
make build && make run
```

체크: File > Connect to Server…(⇧⌘K) → 시트 뜸. 사이드바 "+ Connect…"로도 같은 시트. 필드 채우고 Connect → 에러 나면 빨간 문구. 정상 연결 시 새 탭 생성.

- [ ] **Step 5: Commit**

```bash
git add apps/
git commit -m "feat(ui): Connect sheet + ⇧⌘K menu entry

ConnectSheetView collects user@host:port/path + auth mode (agent vs
key file) + optional ProxyCommand + optional 'Save to ssh_config'
nickname. 'Browse…' opens NSOpenPanel scoped to ~/.ssh for key
selection. Accepts ssh://user@host:port/path URLs pasted into the
server field.

Wired to ⇧⌘K via File menu (distinct from the ⌘K palette) plus the
sidebar '+ Connect…' row. Performs append→resolve→connect→newRemoteTab
on submit; connection errors render inline in the sheet so the user
can edit and retry without losing state."
```

---

## Task 17: TOFU / Changed-key / Passphrase alert wiring

**Files:**
- Modify: `apps/Sources/Services/SshPoolService.swift` (wire callbacks through FFI)
- Modify: `crates/cairn-ffi/src/ssh.rs` (Swift callback plumbing)
- Modify: `apps/Sources/Services/HostKeyAlertResolver.swift`, `PassphraseResolver.swift` (if refinements needed)

> This task completes the FFI callback plumbing deferred in Task 8. Run after the Swift shell is in place so the pieces can be wired and tested end-to-end.

- [ ] **Step 1: swift-bridge Swift-side callback object**

In `SshPoolService.swift`, add types that match the Rust extern "Swift" declarations from Task 8:

```swift
final class HostKeyCallback {
    let resolver: HostKeyAlertResolver
    init(resolver: HostKeyAlertResolver) { self.resolver = resolver }

    /// Called from Rust during russh handshake. Blocks the Rust thread until
    /// the user dismisses the alert.
    func ask(host: String, port: UInt16, offer: HostKeyOffer, knownState: String) -> String {
        let sem = DispatchSemaphore(value: 0)
        var result = "reject"
        Task {
            result = await resolver.resolve(host: host, port: port, offer: offer, knownState: knownState)
            sem.signal()
        }
        sem.wait()
        return result
    }
}

final class PassphraseCallback {
    let resolver: PassphraseResolver
    init(resolver: PassphraseResolver) { self.resolver = resolver }

    func ask(keyPath: String) -> String? {
        let sem = DispatchSemaphore(value: 0)
        var result: String? = nil
        Task {
            result = await resolver.resolve(keyPath: keyPath)
            sem.signal()
        }
        sem.wait()
        return result
    }
}
```

Extend `SshPoolService.connect(...)` to pass both callbacks into `pool.connect`. The Rust-side `ssh_pool_connect` now takes `(spec, hostkey_cb, passphrase_cb)`.

- [ ] **Step 2: Rust-side FFI glue**

Fill in the Rust stubs in `crates/cairn-ffi/src/ssh.rs`:

```rust
fn ssh_pool_connect(
    pool: &SshPoolBridge,
    spec: ffi::ConnectSpecBridge,
    hostkey_cb: ffi::HostKeyCallback,
    passphrase_cb: ffi::PassphraseCallback,
) -> Result<ffi::ConnKeyBridge, String> {
    let rust_hk = Arc::new(SwiftHostKeyAdapter { cb: hostkey_cb });
    let rust_pp = Arc::new(SwiftPassphraseAdapter { cb: passphrase_cb });
    let spec_rust = ssh::ConnectSpec {
        host_alias: spec.host_alias,
        user_override: spec.user_override,
        port_override: spec.port_override,
        identity_file_override: spec.identity_file_override.map(std::path::PathBuf::from),
        proxy_command_override: spec.proxy_command_override,
    };
    runtime().block_on(async {
        pool.inner.connect(spec_rust, rust_hk, rust_pp).await
    })
    .map(|key| ffi::ConnKeyBridge {
        user: key.user,
        hostname: key.hostname,
        port: key.port,
        config_hash_hex: hex::encode(key.config_hash),
    })
    .map_err(|e| e.to_string())
}

struct SwiftHostKeyAdapter { cb: ffi::HostKeyCallback }
#[async_trait::async_trait]
impl ssh::HostKeyResolver for SwiftHostKeyAdapter {
    async fn resolve(&self, host: &str, port: u16, offered_algo: &str, offered_blob: &[u8], known: ssh::KnownResult) -> ssh::TofuDecision {
        use base64::prelude::*;
        let offer = ffi::HostKeyOffer {
            algorithm: offered_algo.into(),
            blob_base64: BASE64_STANDARD.encode(offered_blob),
            fingerprint: ssh::sha256_fingerprint(offered_blob),
        };
        let state = match known {
            ssh::KnownResult::Match               => "match",
            ssh::KnownResult::NotFound            => "not_found",
            ssh::KnownResult::Mismatch { .. }     => "mismatch",
        };
        let result = self.cb.ask_host_key(host.into(), port, offer, state.into());
        match result.as_str() {
            "accept"      => ssh::TofuDecision::Accept,
            "accept_save" => ssh::TofuDecision::AcceptAndSave,
            _             => ssh::TofuDecision::Reject,
        }
    }
}

struct SwiftPassphraseAdapter { cb: ffi::PassphraseCallback }
#[async_trait::async_trait]
impl ssh::PassphraseResolver for SwiftPassphraseAdapter {
    async fn resolve(&self, key_path: &std::path::Path) -> Option<String> {
        self.cb.ask_passphrase(key_path.to_string_lossy().into_owned())
    }
}
```

Add `hex = "0.4"` to `crates/cairn-ffi/Cargo.toml`.

- [ ] **Step 3: 바인딩 재생성 + 빌드**

```bash
./scripts/gen-bindings.sh
make swift 2>&1 | tail -20
```

Expected: BUILD SUCCEEDED.

- [ ] **Step 4: 수동 TOFU 시나리오 테스트**

```bash
# Remove a known_hosts entry to force TOFU
ssh-keygen -R prod-api 2>/dev/null
make run
# In Cairn, click prod-api in sidebar → TOFU alert should appear
# → Accept & Save → tab opens → ssh-keygen -l -f ~/.ssh/known_hosts entry exists
```

- [ ] **Step 5: Commit**

```bash
git add apps/ crates/
git commit -m "feat(ssh): wire host-key + passphrase callbacks from russh to NSAlert

SwiftHostKeyAdapter / SwiftPassphraseAdapter implement the Rust
HostKeyResolver / PassphraseResolver traits by calling back into
Swift via swift-bridge extern types. Swift side uses a
DispatchSemaphore to block the FFI thread while the NSAlert runs on
the main actor — acceptable for v1 since connects happen on detached
tasks.

TOFU alert presents SHA256 fingerprint + algorithm; returns one of
accept / accept_save / reject. Changed-key presents only Cancel.
Passphrase alert checks Keychain first."
```

---

## Task 18: Tab chrome — SSH 배지 + scheme breadcrumb

**Files:**
- Modify: `apps/Sources/Views/Tabs/TabChip.swift`
- Modify: `apps/Sources/Views/BreadcrumbBar.swift`
- Modify: `apps/Sources/ContentView.swift` (window title)

- [ ] **Step 1: `TabChip.swift` — 배지 추가**

Add a computed `protocolBadge: String?` that returns `"SSH"` when `tab.provider.identifier` is `.ssh(_)`, else nil. Render it as a small teal-tinted capsule before the title:

```swift
HStack(spacing: 6) {
    if let badge = tab.protocolBadge {
        Text(badge)
            .font(.system(size: 9, weight: .semibold))
            .foregroundStyle(Color(red: 0.66, green: 0.94, blue: 0.84))
            .padding(.horizontal, 5).padding(.vertical, 1)
            .background(Color(red: 0.39, green: 0.78, blue: 0.70).opacity(0.25))
            .clipShape(RoundedRectangle(cornerRadius: 3))
    }
    Text(titleText).lineLimit(1).truncationMode(.middle)
}
```

Where `tab.protocolBadge` is a Tab extension:

```swift
extension Tab {
    var protocolBadge: String? {
        if case .ssh = provider.identifier { return "SSH" }
        return nil
    }
    var titleText: String {
        guard let path = currentPath else { return "Tab" }
        if case .ssh(let t) = path.provider {
            let name = path.lastComponent.isEmpty ? "/" : path.lastComponent
            return "\(t.hostname):\(name)"
        }
        return path.lastComponent
    }
}
```

- [ ] **Step 2: `BreadcrumbBar.swift` — scheme 접두사**

```swift
// Render based on tab.currentPath.provider
switch tab.currentPath?.provider {
case .local, .none:
    // existing segments
case .ssh(let t):
    HStack(spacing: 4) {
        Text("ssh://")
            .foregroundStyle(Color(red: 0.55, green: 0.85, blue: 0.73))
            .font(.system(size: 11, weight: .medium, design: .monospaced))
        Text("\(t.user)@\(t.hostname)")
            .foregroundStyle(.primary)
            .font(.system(size: 11, weight: .semibold, design: .monospaced))
        // then path segments
        ForEach(pathSegments, id: \.self) { segment in
            // ... existing segment rendering
        }
    }
}
```

- [ ] **Step 3: window title**

`ContentView.body`:

```swift
.navigationTitle(tab.map { t in
    if case .ssh(let target) = t.currentPath?.provider {
        let name = t.currentPath?.lastComponent ?? "/"
        return "\(target.hostname) · \(name)"
    } else {
        return t.currentFolder?.lastPathComponent ?? "Cairn"
    }
} ?? "Cairn")
```

- [ ] **Step 4: 빌드 + 수동 확인**

```bash
make run
```

Remote 탭 열었을 때:
- 탭 chip 왼쪽에 teal `SSH` 배지
- breadcrumb가 `ssh://user@host / var / log / nginx`
- window title이 `prod-api · nginx`

- [ ] **Step 5: Commit**

```bash
git add apps/
git commit -m "feat(ui): tab chip SSH badge + ssh:// scheme breadcrumb + window title

Tab.protocolBadge returns 'SSH' for .ssh(target) identifiers so the
chip renders a small teal capsule before the title. TabChip title
format for remote tabs: 'host:foldername' (middle-truncating).

BreadcrumbBar prefixes remote paths with a teal 'ssh://user@host'
segment; path segments render in the existing mono style. Window
title follows Finder convention: 'host · foldername' for remote."
```

---

## Task 19: FileListCoordinator provider-dispatched ops + 원격 delete 모달

**Files:**
- Modify: `apps/Sources/Views/FileList/FileListCoordinator.swift`
- Create: `apps/Sources/Views/Alerts/RemoteDeleteConfirm.swift`

- [ ] **Step 1: Provider-aware rename**

Modify `controlTextDidEndEditing`:

```swift
// Build target URLs as FSPath
let oldPath = FSPath(provider: folder.currentPath?.provider ?? .local, path: entry.path.toString())
guard let parent = oldPath.parent() else { return }
let newPath = parent.appending(newName)

// Same-provider check (don't allow rename-across-provider)
guard oldPath.provider == newPath.provider else { return }

Task { [weak self] in
    guard let self else { return }
    do {
        try await provider.rename(from: oldPath, to: newPath)
        self.registerRenameUndo(from: oldPath, to: newPath, provider: self.provider)
        self.onMoved()
    } catch {
        await MainActor.run {
            tf.stringValue = oldName
            NSSound.beep()
        }
    }
}
```

The coordinator needs a reference to `provider` — add it to `init` alongside `folder`:

```swift
private var provider: FileSystemProvider

init(folder: FolderModel,
     provider: FileSystemProvider,
     onActivate: @escaping (FileEntry) -> Void,
     ...) {
    self.folder = folder
    self.provider = provider
    // ...
}
```

Update `FileListView.makeCoordinator` + `updateBindings` to pass the tab's provider.

- [ ] **Step 2: Provider-aware delete + remote confirmation**

```swift
func deleteSelected() {
    guard let table else { return }
    let indexes = table.selectedRowIndexes
    guard !indexes.isEmpty else { return }
    let victims = indexes.compactMap { idx -> FileEntry? in
        idx < lastSnapshot.count ? lastSnapshot[idx] : nil
    }
    let paths = victims.map { FSPath(provider: provider.identifier, path: $0.path.toString()) }

    if case .ssh = provider.identifier {
        confirmRemoteDelete(items: victims) { [weak self] confirmed in
            guard let self, confirmed else { return }
            Task {
                do {
                    try await self.provider.delete(paths)
                    self.onMoved()
                } catch {
                    await MainActor.run { NSSound.beep() }
                }
            }
        }
    } else {
        // Local: existing trash + undo path
        // ... (unchanged)
    }
}
```

- [ ] **Step 3: `RemoteDeleteConfirm.swift` — modal**

```swift
import AppKit

enum RemoteDeleteConfirm {
    static var dontAskThisSession = false   // Tab-scoped reset on app restart

    static func present(hostSummary: String, parent: String, names: [String], completion: @escaping (Bool) -> Void) {
        if dontAskThisSession { completion(true); return }

        let alert = NSAlert()
        alert.alertStyle = .warning
        alert.messageText = "Delete \(names.count) item\(names.count == 1 ? "" : "s") permanently?"
        let list = names.prefix(5).joined(separator: "\n • ")
        let more = names.count > 5 ? "\n + \(names.count - 5) more" : ""
        alert.informativeText = "From \(hostSummary):\(parent)\n • \(list)\(more)\n\nThis cannot be undone."
        alert.addButton(withTitle: "Delete")
        alert.addButton(withTitle: "Cancel")

        let check = NSButton(checkboxWithTitle: "Don't ask again for this session", target: nil, action: nil)
        alert.accessoryView = check

        let res = alert.runModal()
        if check.state == .on { dontAskThisSession = true }
        completion(res == .alertFirstButtonReturn)
    }
}
```

Coordinator's `confirmRemoteDelete`:

```swift
private func confirmRemoteDelete(items: [FileEntry], completion: @escaping (Bool) -> Void) {
    let hostSummary: String = {
        if case .ssh(let t) = provider.identifier { return "\(t.user)@\(t.hostname)" }
        return ""
    }()
    let parent = folder.currentPath?.path ?? "/"
    RemoteDeleteConfirm.present(hostSummary: hostSummary, parent: parent,
                                 names: items.map { $0.name.toString() },
                                 completion: completion)
}
```

- [ ] **Step 4: Provider-aware mkdir (context menu "New Folder")**

Add a menu item in `menu(for:)` / `emptySpaceMenu` that calls:

```swift
@objc private func menuNewFolder(_ sender: Any?) {
    guard let parent = folder.currentPath else { return }
    let target = parent.appending("untitled folder")
    Task { [weak self] in
        guard let self else { return }
        do { try await self.provider.mkdir(target); self.onMoved() }
        catch { await MainActor.run { NSSound.beep() } }
    }
}
```

- [ ] **Step 5: 빌드 + 수동 확인**

```bash
make run
```

로컬 탭: rename / delete / 기존 동작 유지.
원격 탭: rename (Enter) → 서버 파일 이름 변경. delete (⌘⌫) → 확인 모달 → Delete 클릭 → 서버에서 제거. mkdir 메뉴 → 새 폴더 생성.

- [ ] **Step 6: Commit**

```bash
git add apps/
git commit -m "feat(filelist): provider-dispatched rename/delete/mkdir + remote delete modal

FileListCoordinator now takes a FileSystemProvider reference and
routes rename/delete/mkdir through it. Local paths keep the existing
trash+undo behavior; remote deletions go through a confirmation
modal (Delete permanently?) with per-session 'don't ask' escape.

Multi-selection delete shows up to 5 names + count. Same-provider
guard on rename prevents accidental cross-provider moves (those must
go through Transfer — Task 22)."
```

---

## Task 20: `TransferController` + `TransferJob` + 호스트당 serial 실행

**Files:**
- Create: `apps/Sources/Services/TransferController.swift`
- Create: `apps/Sources/Services/TransferJob.swift`
- Create: `apps/CairnTests/SSH/TransferControllerTests.swift`

- [ ] **Step 1: `TransferJob.swift`**

```swift
import Foundation

struct TransferJob: Identifiable, Equatable {
    let id = UUID()
    let source: FSPath
    let destination: FSPath
    let sizeHint: Int64?
    var bytesCompleted: Int64 = 0
    var state: State = .queued
    var startedAt: Date? = nil
    var finishedAt: Date? = nil
    let cancel: CancelToken

    enum State: Equatable {
        case queued
        case running
        case completed
        case cancelled
        case failed(String)
    }

    /// Percentage 0...100 or nil if size unknown.
    var percent: Double? {
        guard let s = sizeHint, s > 0 else { return nil }
        return min(100, Double(bytesCompleted) / Double(s) * 100)
    }

    /// Bytes-per-second; nil until first second of running.
    var speed: Double? {
        guard let start = startedAt, state == .running else { return nil }
        let elapsed = Date().timeIntervalSince(start)
        guard elapsed > 0.5 else { return nil }
        return Double(bytesCompleted) / elapsed
    }

    var eta: TimeInterval? {
        guard let s = sizeHint, let sp = speed, sp > 0, s > bytesCompleted else { return nil }
        return Double(s - bytesCompleted) / sp
    }

    /// Host key for serial scheduling. nil = local-only transfer, runs in
    /// its own lane.
    var remoteHostKey: String? {
        if case .ssh(let t) = source.provider { return "\(t.user)@\(t.hostname):\(t.port)" }
        if case .ssh(let t) = destination.provider { return "\(t.user)@\(t.hostname):\(t.port)" }
        return nil
    }
}
```

- [ ] **Step 2: `TransferController.swift`**

```swift
import Foundation
import Observation

@Observable
final class TransferController {
    private(set) var jobs: [TransferJob] = []

    /// One active worker per remote host. `nil` key = local lane.
    private var runningPerHost: [String?: UUID] = [:]

    var activeCount: Int {
        jobs.filter { $0.state == .queued || $0.state == .running }.count
    }

    var hasActive: Bool { activeCount > 0 }

    func enqueue(source: FSPath, destination: FSPath, sizeHint: Int64?,
                 execute: @escaping (_ job: TransferJob, _ progress: @escaping (Int64) -> Void) async throws -> Void) {
        var job = TransferJob(source: source, destination: destination, sizeHint: sizeHint, cancel: CancelToken())
        jobs.append(job)
        Task { await maybeStartNext() }

        // Store executor keyed by job id for the host-serializer to pick up.
        pendingExecutors[job.id] = execute
    }

    func cancel(_ id: UUID) {
        guard let idx = jobs.firstIndex(where: { $0.id == id }) else { return }
        jobs[idx].cancel.cancel()
        if jobs[idx].state == .queued {
            jobs[idx].state = .cancelled
            jobs[idx].finishedAt = Date()
        }
    }

    func retry(_ id: UUID) {
        guard let idx = jobs.firstIndex(where: { $0.id == id }),
              case .failed = jobs[idx].state else { return }
        let old = jobs[idx]
        enqueue(source: old.source, destination: old.destination, sizeHint: old.sizeHint, execute: pendingExecutors[id] ?? { _, _ in })
    }

    func cancelAll() {
        for id in jobs.map(\.id) { cancel(id) }
    }

    private var pendingExecutors: [UUID: (TransferJob, @escaping (Int64) -> Void) async throws -> Void] = [:]

    private func maybeStartNext() async {
        let startable = jobs.enumerated().filter { _, j in
            j.state == .queued && runningPerHost[j.remoteHostKey] == nil
        }
        for (idx, _) in startable {
            await MainActor.run { self.startJob(at: idx) }
        }
    }

    @MainActor
    private func startJob(at idx: Int) {
        guard idx < jobs.count else { return }
        let host = jobs[idx].remoteHostKey
        let id = jobs[idx].id
        guard let executor = pendingExecutors.removeValue(forKey: id) else { return }
        runningPerHost[host] = id
        jobs[idx].state = .running
        jobs[idx].startedAt = Date()
        Task {
            do {
                try await executor(jobs[idx]) { [weak self] bytes in
                    Task { @MainActor in
                        guard let self, let i = self.jobs.firstIndex(where: { $0.id == id }) else { return }
                        self.jobs[i].bytesCompleted = bytes
                    }
                }
                await MainActor.run {
                    if let i = self.jobs.firstIndex(where: { $0.id == id }) {
                        self.jobs[i].state = .completed
                        self.jobs[i].finishedAt = Date()
                    }
                    self.runningPerHost[host] = nil
                    Task { await self.maybeStartNext() }
                }
            } catch is CancellationError {
                await MainActor.run {
                    if let i = self.jobs.firstIndex(where: { $0.id == id }) {
                        self.jobs[i].state = .cancelled
                        self.jobs[i].finishedAt = Date()
                    }
                    self.runningPerHost[host] = nil
                    Task { await self.maybeStartNext() }
                }
            } catch {
                await MainActor.run {
                    if let i = self.jobs.firstIndex(where: { $0.id == id }) {
                        self.jobs[i].state = .failed((error as NSError).localizedDescription)
                        self.jobs[i].finishedAt = Date()
                    }
                    self.runningPerHost[host] = nil
                    Task { await self.maybeStartNext() }
                }
            }
        }
    }
}
```

- [ ] **Step 3: `TransferControllerTests.swift`**

```swift
import XCTest
@testable import Cairn

final class TransferControllerTests: XCTestCase {
    func testSerialPerHost() async throws {
        let controller = TransferController()
        let local = FSPath(provider: .local, path: "/tmp/src")
        let target = SshTarget(user: "u", hostname: "h", port: 22, configHashHex: "0".padding(toLength: 32, withPad: "0", startingAt: 0))
        let remote = FSPath(provider: .ssh(target), path: "/tmp/dst")

        var startOrder: [Int] = []
        let counter = actorCounter()

        for i in 0..<3 {
            controller.enqueue(source: local, destination: remote, sizeHint: 10) { _, progress in
                await counter.record(i)
                try await Task.sleep(nanoseconds: 50_000_000)
            }
        }
        try await Task.sleep(nanoseconds: 300_000_000)

        startOrder = await counter.all()
        XCTAssertEqual(startOrder.count, 3)  // all ran
        // Serial order means indexes observed in sequence: 0 < 1 < 2 in recorded time
        XCTAssertEqual(startOrder, [0, 1, 2])
    }

    func testCancelQueued() {
        let controller = TransferController()
        let p = FSPath(provider: .local, path: "/x")
        controller.enqueue(source: p, destination: p, sizeHint: nil) { _, _ in
            try await Task.sleep(nanoseconds: 1_000_000_000)
        }
        let id = controller.jobs.last!.id
        controller.cancel(id)
        // Give microtask a tick
        let exp = expectation(description: "cancelled")
        DispatchQueue.main.async {
            if controller.jobs.last?.state == .cancelled { exp.fulfill() }
        }
        wait(for: [exp], timeout: 0.2)
    }
}

private func actorCounter() -> SerialCounter { SerialCounter() }
actor SerialCounter {
    private var order: [Int] = []
    func record(_ i: Int) { order.append(i) }
    func all() -> [Int] { order }
}
```

- [ ] **Step 4: 테스트**

```bash
xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | grep Transfer | tail -10
```

Expected: 2 passed.

- [ ] **Step 5: Commit**

```bash
git add apps/
git commit -m "feat(transfer): TransferController with per-host serial scheduling

TransferJob captures source/destination FSPath, bytesCompleted,
state, timing, and a CancelToken. percent/speed/eta are computed.
remoteHostKey returns a 'user@host:port' string for scheduling; nil
means local-only (own lane).

TransferController is @Observable. enqueue() queues a job with an
async executor closure; runningPerHost ensures at most one job per
host runs at a time. maybeStartNext() is called on enqueue and on
each job's finish. cancel() sets the CancelToken and transitions
queued jobs to .cancelled immediately; running jobs abort at the
next cancel checkpoint."
```

---

## Task 21: `TransferHudChip` + `TransferPopoverView` + pulse

**Files:**
- Create: `apps/Sources/Views/Transfer/TransferHudChip.swift`
- Create: `apps/Sources/Views/Transfer/TransferPopoverView.swift`
- Create: `apps/Sources/Views/Transfer/TransferJobRow.swift`
- Modify: `apps/Sources/ContentView.swift` (toolbar에 chip 배치)

- [ ] **Step 1: `TransferHudChip.swift`**

```swift
import SwiftUI

struct TransferHudChip: View {
    @Bindable var controller: TransferController
    @State private var popoverOpen: Bool = false
    @State private var pulseToken: UUID = UUID()

    var body: some View {
        Group {
            if controller.hasActive {
                chip
                    .onTapGesture { popoverOpen.toggle() }
                    .popover(isPresented: $popoverOpen, arrowEdge: .top) {
                        TransferPopoverView(controller: controller)
                    }
                    .onChange(of: controller.activeCount) { oldValue, newValue in
                        if newValue > oldValue { pulseToken = UUID() }
                    }
                    .scaleEffect(pulseToken == pulseToken ? 1.0 : 1.0)     // keep still
                    .modifier(PulseOnToken(token: pulseToken))
            }
        }
    }

    private var chip: some View {
        HStack(spacing: 5) {
            ProgressView().controlSize(.mini).tint(Color(red: 0.55, green: 0.85, blue: 0.73))
            Text("↕ \(controller.activeCount)")
                .font(.system(size: 11, weight: .semibold))
                .foregroundStyle(Color(red: 0.72, green: 0.94, blue: 0.85))
        }
        .padding(.horizontal, 8).padding(.vertical, 3)
        .background(
            RoundedRectangle(cornerRadius: 5)
                .fill(Color(red: 0.55, green: 0.85, blue: 0.73).opacity(0.18))
                .overlay(
                    RoundedRectangle(cornerRadius: 5)
                        .stroke(Color(red: 0.55, green: 0.85, blue: 0.73).opacity(0.35))
                )
        )
    }
}

/// One-second pulse that fires when `token` changes.
struct PulseOnToken: ViewModifier {
    let token: UUID
    @State private var scale: CGFloat = 1.0
    func body(content: Content) -> some View {
        content.scaleEffect(scale)
            .onChange(of: token) { _, _ in
                withAnimation(.easeOut(duration: 0.15)) { scale = 1.15 }
                DispatchQueue.main.asyncAfter(deadline: .now() + 0.15) {
                    withAnimation(.easeIn(duration: 0.3)) { scale = 1.0 }
                }
            }
    }
}
```

- [ ] **Step 2: `TransferJobRow.swift`**

```swift
import SwiftUI

struct TransferJobRow: View {
    let job: TransferJob
    var onCancel: () -> Void
    var onRetry: () -> Void

    var body: some View {
        VStack(alignment: .leading, spacing: 3) {
            HStack {
                Text(directionIcon).font(.system(size: 12))
                Text(job.source.lastComponent).lineLimit(1).truncationMode(.middle)
                Spacer()
                rightSide
            }.font(.system(size: 11))

            if job.state == .running, let pct = job.percent {
                ProgressView(value: pct, total: 100)
                    .progressViewStyle(.linear)
                    .tint(.green)
            } else if job.state == .queued {
                Text("Queued")
                    .font(.system(size: 9, weight: .semibold))
                    .foregroundStyle(.secondary)
                    .padding(.horizontal, 4).padding(.vertical, 1)
                    .background(Color.secondary.opacity(0.2))
                    .clipShape(RoundedRectangle(cornerRadius: 2))
            }

            HStack(spacing: 8) {
                Text(destinationSummary).font(.system(size: 10)).foregroundStyle(.secondary)
                if let sp = job.speed { Text(ByteCountFormatter().string(fromByteCount: Int64(sp)) + "/s").font(.system(size: 10)).foregroundStyle(.secondary) }
                if let eta = job.eta { Text("ETA " + formatDuration(eta)).font(.system(size: 10)).foregroundStyle(.secondary) }
                Spacer()
                if job.state == .failed(_) || job.state == .cancelled {
                    Button("Retry", action: onRetry).buttonStyle(.link).font(.system(size: 10))
                }
                if job.state == .running || job.state == .queued {
                    Button("Cancel", action: onCancel).buttonStyle(.link).font(.system(size: 10))
                }
            }
        }
        .padding(.vertical, 4)
    }

    private var directionIcon: String {
        if case .ssh = job.source.provider, case .local = job.destination.provider { return "⬇" }
        if case .local = job.source.provider, case .ssh = job.destination.provider { return "⬆" }
        return "⇄"
    }

    private var rightSide: some View {
        Group {
            switch job.state {
            case .completed: Text("✓").foregroundStyle(.green)
            case .cancelled: Text("✕").foregroundStyle(.secondary)
            case .failed:    Text("⚠︎").foregroundStyle(.red)
            case .running:   if let p = job.percent { Text("\(Int(p))%").foregroundStyle(.primary) }
            case .queued:    EmptyView()
            }
        }.font(.system(size: 11, weight: .semibold))
    }

    private var destinationSummary: String {
        switch job.destination.provider {
        case .ssh(let t): return "to \(t.hostname):\(job.destination.parent()?.path ?? "/")"
        case .local:      return "to \(job.destination.parent()?.path ?? "/")"
        }
    }

    private func formatDuration(_ seconds: TimeInterval) -> String {
        let s = Int(seconds)
        if s < 60 { return "\(s)s" }
        if s < 3600 { return "\(s / 60)m \(s % 60)s" }
        return "\(s / 3600)h \(s % 3600 / 60)m"
    }
}
```

- [ ] **Step 3: `TransferPopoverView.swift`**

```swift
import SwiftUI

struct TransferPopoverView: View {
    @Bindable var controller: TransferController

    var active: [TransferJob] { controller.jobs.filter { $0.state == .running || $0.state == .queued } }
    var recent: [TransferJob] { controller.jobs.filter { j in
        switch j.state { case .completed, .cancelled, .failed: return true; default: return false }
    }.suffix(10).reversed() }

    var body: some View {
        VStack(alignment: .leading, spacing: 0) {
            if !active.isEmpty {
                section(title: "Active Transfers")
                ForEach(active) { job in
                    TransferJobRow(job: job,
                                   onCancel: { controller.cancel(job.id) },
                                   onRetry: { controller.retry(job.id) })
                        .padding(.horizontal, 10)
                    Divider().padding(.leading, 10)
                }
            }
            if !recent.isEmpty {
                section(title: "Recent")
                ForEach(recent) { job in
                    TransferJobRow(job: job,
                                   onCancel: { controller.cancel(job.id) },
                                   onRetry: { controller.retry(job.id) })
                        .padding(.horizontal, 10)
                    Divider().padding(.leading, 10)
                }
            }
        }
        .padding(.vertical, 8)
        .frame(minWidth: 340)
    }

    private func section(title: String) -> some View {
        Text(title.uppercased())
            .font(.system(size: 9, weight: .bold))
            .foregroundStyle(.secondary)
            .padding(.horizontal, 10).padding(.top, 4).padding(.bottom, 2)
    }
}
```

- [ ] **Step 4: `ContentView.swift` — toolbar 통합**

```swift
ToolbarItem(placement: .primaryAction) {
    TransferHudChip(controller: app.transfers)
}
```

- [ ] **Step 5: 빌드 + 수동 smoke**

```bash
make run
```

드래그-드랍 없이 직접 테스트 트리거할 방법은 Task 22 이후. 일단 컴파일만 확인.

- [ ] **Step 6: Commit**

```bash
git add apps/
git commit -m "feat(transfer): toolbar HUD chip + popover with per-job progress

TransferHudChip appears only when controller.hasActive, shows an
animated spinner + '↕ N' count in teal glass. Click toggles a popover
anchored to the chip.

TransferPopoverView splits into Active (running + queued) and Recent
(last 10 completed/cancelled/failed in reverse chrono order). Each
row: direction icon + filename, progress bar or 'Queued' badge,
destination summary, speed + ETA, Cancel/Retry links depending on
state.

PulseOnToken modifier pulses the chip (1.0 → 1.15 → 1.0, 450ms) when
activeCount rises — the drop-feedback hint agreed in spec §4.5."
```

---

## Task 22: Cross-provider drag-drop integration

**Files:**
- Modify: `apps/Sources/Views/FileList/FileListNSTableView.swift`
- Modify: `apps/Sources/Views/FileList/FileListCoordinator.swift`

- [ ] **Step 1: 드롭 accept 범위 확장**

`FileListNSTableView`는 이미 `.fileURL` pasteboard type을 등록. 로컬→원격 드랍은 source URL(s) = local, destination row = remote 폴더 또는 tab의 currentPath.

Modify `tableView(_:acceptDrop:row:dropOperation:)` in `FileListCoordinator`:

```swift
func tableView(_ tableView: NSTableView, acceptDrop info: NSDraggingInfo, row: Int, dropOperation op: NSTableView.DropOperation) -> Bool {
    guard op == .on, row >= 0, row < lastSnapshot.count else { return false }
    let target = lastSnapshot[row]
    guard target.kind == .Directory else { return false }
    let targetPath = FSPath(provider: provider.identifier, path: target.path.toString())

    guard let urls = info.draggingPasteboard.readObjects(forClasses: [NSURL.self], options: nil) as? [URL] else {
        return false
    }

    // Classify source: all URLs are local file URLs on macOS drag boards;
    // remote tabs expose their rows as fileURLs representing the local cache
    // copy (when dragged to local) OR we block drag-out-of-remote in v1.
    // v1 policy: drag-to-remote is supported; drag-from-remote to local uses
    // the cache path, enqueueing a download first. Cross-provider detection:
    let sourceIsLocal = urls.allSatisfy { $0.isFileURL }    // on macOS pasteboards, always true

    // If destination provider is local, FileManager.moveItem handles it.
    // If destination is SSH, enqueue upload transfers.
    switch provider.identifier {
    case .local:
        // Existing local drop logic
        return localAcceptDrop(info, row: row, op: op)
    case .ssh:
        var count = 0
        for src in urls {
            let dst = targetPath.appending(src.lastPathComponent)
            let size = try? FileManager.default.attributesOfItem(atPath: src.path)[.size] as? NSNumber
            app.transfers.enqueue(
                source: FSPath(provider: .local, path: src.path),
                destination: dst,
                sizeHint: size?.int64Value
            ) { [weak self] job, progress in
                guard let self else { return }
                try await self.provider.uploadFromLocal(src, to: dst, progress: progress, cancel: job.cancel)
            }
            count += 1
        }
        // Reload destination folder after transfers enqueued (visible once completed)
        if count > 0 { DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) { self.onMoved() } }
        return count > 0
    }
}
```

> `app` is not a direct coordinator property today — inject `transfers: TransferController` + `provider` during `updateBindings`. Existing `onMoved` / `onActivate` pattern is the model for this.

Add a remote→local path: tables exporting rows on remote tabs write the SFTP metadata to pasteboard as well, so Finder/other Cairn tabs can drag out. For v1, use a serialized `FSPath` JSON as a custom pasteboard type; receiving side recognizes it and enqueues a download job.

```swift
// tableView(_:pasteboardWriterForRow:) — add custom type for remote rows
func tableView(_ tableView: NSTableView, pasteboardWriterForRow row: Int) -> NSPasteboardWriting? {
    guard row >= 0, row < lastSnapshot.count else { return nil }
    let entry = lastSnapshot[row]
    let path = FSPath(provider: provider.identifier, path: entry.path.toString())
    switch provider.identifier {
    case .local:
        return URL(fileURLWithPath: entry.path.toString()) as NSURL
    case .ssh:
        let item = NSPasteboardItem()
        if let data = try? JSONEncoder().encode(path) {
            item.setData(data, forType: NSPasteboard.PasteboardType("com.cairn.fspath"))
        }
        return item
    }
}

// Register the custom type in makeNSView
table.registerForDraggedTypes([.fileURL, NSPasteboard.PasteboardType("com.cairn.fspath")])
```

And the accept path needs to read both types:

```swift
if let data = info.draggingPasteboard.data(forType: NSPasteboard.PasteboardType("com.cairn.fspath")),
   let remotePath = try? JSONDecoder().decode(FSPath.self, from: data) {
    // Remote→anywhere: download to a cache path first, then handle as local move
    // For same-provider remote→remote: use provider.copyInPlace
}
```

- [ ] **Step 2: 빌드 + 수동 테스트**

```bash
make run
```

1. 로컬 탭에서 파일 drag → 원격 탭 폴더에 drop → toolbar chip 나타남 + pulse → 잠시 후 원격 폴더에 나타남
2. 원격 탭에서 파일 drag → 로컬 탭 folder에 drop → chip 나타남 → 로컬에 파일 나타남
3. 같은 원격 내 drag → copyInPlace (server-side) or fallback 동작

- [ ] **Step 3: Commit**

```bash
git add apps/
git commit -m "feat(filelist): cross-provider drag-drop via TransferController

FileListNSTableView registers a custom pasteboard type
'com.cairn.fspath' for remote rows (carries the FSPath JSON) in
addition to the existing .fileURL type for local rows.

acceptDrop inspects the destination provider and routes: local→local
through the existing FileManager.moveItem path; local→ssh enqueues
upload TransferJob(s) keyed by host (one serial lane per host);
ssh→local enqueues download jobs; ssh→ssh on the same host uses
provider.copyInPlace (server-side copy when supported, fallback to
client-mediated). Transfers surface in the toolbar HUD popover."
```

---

## Task 23: 텍스트 preview + Quick Look + Open With 캐시

**Files:**
- Create: `apps/Sources/Services/RemoteCacheStore.swift`
- Modify: `apps/Sources/ViewModels/PreviewModel.swift` (provider-aware readHead)
- Modify: `apps/Sources/Views/FileList/FileListNSTableView.swift` (Space → cache → QuickLook)
- Modify: `apps/Sources/Views/FileList/FileListCoordinator.swift` (Open With through cache)
- Modify: `apps/Sources/CairnApp.swift` (File > "Clear Remote Cache" menu entry)

- [ ] **Step 1: `RemoteCacheStore.swift`**

```swift
import Foundation
import CryptoKit

final class RemoteCacheStore {
    static let shared = RemoteCacheStore()

    private let root: URL
    private let limitBytes: Int64 = 500 * 1024 * 1024
    private let lock = NSLock()

    init() {
        let caches = FileManager.default.urls(for: .cachesDirectory, in: .userDomainMask).first!
        let dir = caches.appendingPathComponent("Cairn/remote", isDirectory: true)
        try? FileManager.default.createDirectory(at: dir, withIntermediateDirectories: true)
        self.root = dir
    }

    /// Path inside the cache for a remote file.
    func cacheURL(for path: FSPath) -> URL {
        guard case .ssh(let t) = path.provider else {
            return URL(fileURLWithPath: path.path)
        }
        let hostKey = "\(t.user)@\(t.hostname)-\(t.port)"
        var h = SHA256()
        h.update(data: Data(path.path.utf8))
        let hash = h.finalize().map { String(format: "%02x", $0) }.joined()
        let ext = (path.path as NSString).pathExtension
        let filename = ext.isEmpty ? hash : "\(hash).\(ext)"
        return root.appendingPathComponent(hostKey).appendingPathComponent(filename)
    }

    /// Download the remote file into the cache if missing or stale.
    /// Staleness check: compare remote mtime with local mtime; miss → fetch.
    func fetch(remotePath: FSPath, via provider: FileSystemProvider) async throws -> URL {
        let url = cacheURL(for: remotePath)
        try FileManager.default.createDirectory(at: url.deletingLastPathComponent(),
                                                 withIntermediateDirectories: true)

        let remoteStat = try await provider.stat(remotePath)
        if FileManager.default.fileExists(atPath: url.path) {
            let localAttr = try? FileManager.default.attributesOfItem(atPath: url.path)
            let localMtime = (localAttr?[.modificationDate] as? Date) ?? .distantPast
            if let rm = remoteStat.mtime, rm <= localMtime {
                return url   // fresh
            }
        }

        let cancel = CancelToken()
        try await provider.downloadToLocal(remotePath, toLocalURL: url, progress: { _ in }, cancel: cancel)
        if let rm = remoteStat.mtime {
            try? FileManager.default.setAttributes([.modificationDate: rm], ofItemAtPath: url.path)
        }
        enforceLimit()
        return url
    }

    func clear() {
        lock.lock(); defer { lock.unlock() }
        try? FileManager.default.removeItem(at: root)
        try? FileManager.default.createDirectory(at: root, withIntermediateDirectories: true)
    }

    /// Simple size-based purge (oldest mtime first) until under limit.
    private func enforceLimit() {
        lock.lock(); defer { lock.unlock() }
        var all: [(URL, Int64, Date)] = []
        guard let walker = FileManager.default.enumerator(at: root, includingPropertiesForKeys: [.fileSizeKey, .contentModificationDateKey]) else { return }
        var total: Int64 = 0
        for case let u as URL in walker {
            let v = try? u.resourceValues(forKeys: [.fileSizeKey, .contentModificationDateKey])
            let size = Int64(v?.fileSize ?? 0)
            let mtime = v?.contentModificationDate ?? .distantPast
            all.append((u, size, mtime))
            total += size
        }
        guard total > limitBytes else { return }
        all.sort { $0.2 < $1.2 }    // oldest first
        for (u, size, _) in all {
            if total <= limitBytes { break }
            try? FileManager.default.removeItem(at: u)
            total -= size
        }
    }
}
```

- [ ] **Step 2: `PreviewModel` provider-aware readHead**

Modify load path:

```swift
func loadHead(_ path: FSPath, via provider: FileSystemProvider) async {
    state = .loading
    do {
        let data = try await provider.readHead(path, max: 200 * 1024)
        // Determine text vs binary and render via existing cairn-preview path
        if isLikelyText(data) {
            self.focusedBody = .text(String(data: data, encoding: .utf8) ?? "")
        } else {
            self.focusedBody = .pressSpaceForFullPreview
        }
        state = .loaded
    } catch {
        state = .failed(ErrorMessage.userFacing(error))
    }
}

private func isLikelyText(_ data: Data) -> Bool {
    // Null byte in first 8KB = likely binary (heuristic used by cairn-preview)
    !data.prefix(8192).contains(0)
}
```

`PreviewPaneView` already reads `PreviewModel` — the `.pressSpaceForFullPreview` branch shows a small hint card.

- [ ] **Step 3: Space → Quick Look cache path**

In `FileListCoordinator.snapshotQuickLookURLs`:

```swift
func snapshotQuickLookURLs() async {
    let selectedRows = table?.selectedRowIndexes ?? IndexSet()
    var paths: [URL] = []
    for row in selectedRows {
        guard row < lastSnapshot.count else { continue }
        let entry = lastSnapshot[row]
        let p = FSPath(provider: provider.identifier, path: entry.path.toString())
        if case .local = p.provider {
            paths.append(URL(fileURLWithPath: entry.path.toString()))
        } else {
            // Remote: download to cache (may take a while for large files —
            // user sees the system QL placeholder until we complete).
            if let cached = try? await provider.downloadToCache(p) {
                paths.append(cached)
            }
        }
    }
    if paths.isEmpty, !lastSnapshot.isEmpty {
        let first = lastSnapshot[0]
        let fp = FSPath(provider: provider.identifier, path: first.path.toString())
        if case .local = fp.provider {
            paths = [URL(fileURLWithPath: first.path.toString())]
        } else if let cached = try? await provider.downloadToCache(fp) {
            paths = [cached]
        }
    }
    quickLookSnapshot = paths
}
```

- [ ] **Step 4: Open With → read-only cache copy**

In `menuOpenWith(_:)`:

```swift
@objc private func menuOpenWith(_ sender: NSMenuItem) {
    guard let payload = sender.representedObject as? MenuPayload, let appURL = payload.appURL else { return }
    let p = FSPath(provider: provider.identifier, path: payload.entry.path.toString())
    Task {
        let fileURL: URL
        if case .ssh = p.provider {
            guard let cached = try? await provider.downloadToCache(p) else { return }
            // Mark read-only so user doesn't expect edit-sync
            try? FileManager.default.setAttributes([.posixPermissions: 0o444], ofItemAtPath: cached.path)
            fileURL = cached
        } else {
            fileURL = URL(fileURLWithPath: payload.entry.path.toString())
        }
        NSWorkspace.shared.open([fileURL], withApplicationAt: appURL, configuration: .init()) { _, error in
            if let error { NSLog("cairn: Open With failed — \(error.localizedDescription)") }
        }
    }
}
```

- [ ] **Step 5: "Clear Remote Cache" 메뉴**

In `CairnApp.swift` ViewCommands:

```swift
Divider()
Button("Clear Remote Cache") {
    RemoteCacheStore.shared.clear()
}
```

- [ ] **Step 6: 수동 smoke**

```bash
make run
```

원격 탭에서:
- 텍스트 파일 클릭 → preview pane에 첫 200KB 나옴
- Space → 몇 초 뒤 Quick Look으로 전체 뜸
- 같은 파일 다시 Space → 캐시 hit, 즉시 뜸
- Open With → 기본 앱에서 read-only 파일로 열림
- View 메뉴 "Clear Remote Cache" → 캐시 비움

- [ ] **Step 7: Commit**

```bash
git add apps/
git commit -m "feat(preview): text head streaming + lazy QuickLook cache + Open With

RemoteCacheStore maps remote FSPath to
~/Library/Caches/Cairn/remote/<host>/<sha256-path>.<ext>; stale check
compares remote mtime with local; simple LRU purge when total >
500MB (oldest mtime first). Provider.downloadToCache is the only
entry point; Quick Look snapshotQuickLookURLs + Open With both go
through it.

Preview pane streams up to 200KB head via provider.readHead and
falls back to a 'press Space for preview' hint on binary content.
Cached files for Open With are chmod 0444 to signal read-only
(edit-in-place sync is a M2 feature).

View menu > 'Clear Remote Cache' wipes the cache root."
```

---

## Task 24: 인라인 에러 상태 (connecting / auth failed / keepalive lost / listing failed)

**Files:**
- Create: `apps/Sources/Views/Empty/RemoteErrorCard.swift`
- Modify: `apps/Sources/Views/Empty/EmptyStateView.swift` (permission denied branch extension)
- Modify: `apps/Sources/ContentView.swift` (tab body branching)

- [ ] **Step 1: `RemoteErrorCard.swift`**

```swift
import SwiftUI

struct RemoteErrorCard: View {
    let title: String
    let detail: String
    var actions: [Action] = []

    struct Action {
        let label: String
        let handler: () -> Void
    }

    var body: some View {
        VStack(alignment: .leading, spacing: 10) {
            HStack(spacing: 8) {
                Image(systemName: "exclamationmark.triangle.fill")
                    .foregroundStyle(.red)
                Text(title).font(.headline)
            }
            Text(detail)
                .font(.system(.body, design: .monospaced))
                .foregroundStyle(.secondary)
                .textSelection(.enabled)
            HStack(spacing: 8) {
                ForEach(Array(actions.enumerated()), id: \.offset) { _, a in
                    Button(a.label) { a.handler() }
                }
            }
        }
        .padding(20)
        .background(RoundedRectangle(cornerRadius: 8).fill(Color.red.opacity(0.08)))
        .overlay(RoundedRectangle(cornerRadius: 8).stroke(Color.red.opacity(0.3)))
        .padding(24)
    }
}
```

- [ ] **Step 2: Tab에 연결 상태 필드 추가**

```swift
// Tab.swift
@Observable
final class Tab {
    // … existing
    var connectionPhase: ConnectionPhase = .idle

    enum ConnectionPhase: Equatable {
        case idle
        case connecting(detail: String)            // "Opening tunnel via cloudflared…"
        case connected
        case error(title: String, detail: String)  // auth failed / keepalive lost / listing failed
    }
}
```

`SshPoolService.connect` + provider.list errors propagate into `tab.connectionPhase`.

- [ ] **Step 3: `ContentView.contentBody(tab:)` 분기 추가**

```swift
switch tab.connectionPhase {
case .idle, .connected:
    // existing search / folder state logic
case .connecting(let detail):
    VStack(spacing: 12) {
        ProgressView().controlSize(.large)
        Text("Connecting to \(tab.currentPath.map(hostName) ?? "…")")
        Text(detail).font(.caption).foregroundStyle(.secondary)
    }
    .frame(maxWidth: .infinity, maxHeight: .infinity)
case .error(let title, let detail):
    RemoteErrorCard(
        title: title,
        detail: detail,
        actions: [
            .init(label: "Retry") { retryConnect(tab) },
            .init(label: "Edit ssh_config") { revealSshConfig() },
            .init(label: "Open Terminal") { openTerminalFor(tab) },
        ]
    )
}
```

- [ ] **Step 4: ProxyCommand stderr "Copy Error" 버튼**

If the detail contains a ProxyCommand stderr prefix, add an extra `Copy Error` action that writes the full detail to the pasteboard.

- [ ] **Step 5: 빌드 + 수동 smoke**

```bash
make run
```

- 잘못된 alias로 연결 시도 → ConfigResolution 에러 카드
- 잘못된 key file 지정 → AuthNoMethods → "Authentication failed" 카드 + Retry
- 실제 연결 후 (옵션) `sudo pkill -f cloudflared` → Keepalive fail → "Connection lost" 오버레이

- [ ] **Step 6: Commit**

```bash
git add apps/
git commit -m "feat(ui): inline remote connection error states

Tab.connectionPhase captures idle / connecting(detail) / connected /
error(title, detail). SshPoolService.connect sets .connecting with a
ProxyCommand-aware detail ('Opening tunnel via cloudflared…' when a
proxy is present) and transitions to .connected or .error.

Inline error card (RemoteErrorCard) exposes Retry / Edit ssh_config /
Open Terminal / Copy Error actions depending on failure class.
Keepalive failures surface a 'Connection lost' overlay with
Reconnect + Close Tab. ProxyCommand stderr preview is monospaced +
selectable for easy inspection."
```

---

## Task 25: Integration 테스트 + ssh_config FSEvents → 사이드바 자동 reload

**Files:**
- Create: `crates/cairn-ssh/tests/integration/mod.rs`
- Create: `crates/cairn-ssh/tests/integration/sftp_roundtrip.rs`
- Modify: (verification step) — ensure `ConfigFileWatcher` actually re-populates sidebar after disk edit

- [ ] **Step 1: Integration test harness (optional, `CAIRN_SSH_IT=1` gated)**

```rust
// crates/cairn-ssh/tests/integration/mod.rs
#![cfg(integration_ssh)]

fn skip_unless_env() {
    if std::env::var("CAIRN_SSH_IT").ok().as_deref() != Some("1") {
        println!("skip: set CAIRN_SSH_IT=1 to run");
        std::process::exit(0);
    }
}
```

```rust
// crates/cairn-ssh/tests/integration/sftp_roundtrip.rs
#![cfg(integration_ssh)]
use cairn_ssh::{SshPool, ConnectSpec};

#[tokio::test]
async fn upload_download_roundtrip() {
    super::skip_unless_env();

    // This test assumes a running openssh-server at localhost:2222 with
    // user 'test' / password-less key 'tests/fixtures/test_ed25519' in agent.
    // CI: docker run -d --name cairn-ssh-it -p 2222:22 \
    //     -v $(pwd)/tests/fixtures/authorized_keys:/home/test/.ssh/authorized_keys \
    //     linuxserver/openssh-server
    // Local: docker-compose up from crates/cairn-ssh/tests/fixtures/compose.yml

    let pool = SshPool::new();
    // TODO: plumb the real hostkey / passphrase resolvers for this test env.
    // This is the implementer's job; the fixture directory should include a
    // known_hosts entry so the test auto-accepts without prompting.
}
```

> **Note to implementer:** full integration test harness is deferred to a follow-on task — the stub above is the scaffolding. For v1 release, run through the Task 26 manual DoD instead. CI integration of Dockerized openssh is out of scope.

- [ ] **Step 2: FSEvents 자동 reload 수동 검증**

```bash
make run
# In another terminal:
echo "\nHost test-sidebar-reload\n    HostName 1.1.1.1" >> ~/.ssh/config
# Cairn sidebar should show 'test-sidebar-reload' within 1s
# Cleanup:
sed -i '' '/Host test-sidebar-reload/,/HostName 1.1.1.1/d' ~/.ssh/config
```

- [ ] **Step 3: Cargo 구성 — `integration_ssh` cfg flag**

`crates/cairn-ssh/Cargo.toml`:

```toml
[features]
integration = []
```

Run with `cargo test -p cairn-ssh --features integration --tests`.

- [ ] **Step 4: Commit**

```bash
git add crates/cairn-ssh/
git commit -m "test(ssh): integration test scaffolding (CAIRN_SSH_IT=1 gated)

tests/integration/ hosts docker-openssh-dependent SFTP roundtrip
tests. Gated by CAIRN_SSH_IT env + 'integration' cargo feature so
'cargo test --workspace' never requires a running server.

FSEvents sidebar reload verified manually; no automated test because
FSEvents can't be mocked from inside the test process reliably."
```

---

## Task 26: 로컬 CI + 수동 DoD + `git tag phase-2-ssh-v1` + `v0.2.0-alpha.1`

**Files:** 없음 (verification only).

- [ ] **Step 1: 풀 Rust CI**

```bash
cargo fmt --check
cargo clippy --workspace -- -D warnings
cargo test --workspace
```

Expected: all green. Failures → fix inline, recommit under respective task.

- [ ] **Step 2: 풀 Swift CI**

```bash
make swift
xcodebuild test -scheme Cairn -destination "platform=macOS" -derivedDataPath build/DerivedData CODE_SIGNING_REQUIRED=NO CODE_SIGN_IDENTITY="" 2>&1 | tee /tmp/cairn-test.log | tail -40
grep "Test Suite" /tmp/cairn-test.log | grep -E "passed|failed"
```

Expected: 60+ existing + ~15 new tests passed, 0 failed.

- [ ] **Step 3: 수동 DoD checklist — 13 항목**

빌드 + 실행:

```bash
make build && make run
```

체크 (순서대로, 각 PASS 표시):

1. `~/.ssh/config`에 `Host prod-api` 새 블록 추가 → 사이드바 "Remote Hosts"에 1초 내 등장 (ConfigFileWatcher)
2. 사이드바 `prod-api` 클릭 → TOFU 모달 뜸 → "Accept & Save" → 탭 열림, home 디렉터리 listing 표시
3. 로컬 탭에서 파일 drag → `prod-api` 탭의 폴더에 drop → toolbar chip (`↕ 1`) 등장 + pulse → popover 열어 진행률 확인 → 완료 후 원격 폴더에 파일 나타남 + chip 사라짐
4. 원격 파일 선택 → ⏎ → inline rename field 활성 → 새 이름 입력 + ⏎ → 원격에서 이름 변경
5. 원격 파일 선택 → ⌘⌫ → "Delete permanently?" 모달 → Delete → 원격에서 삭제
6. 원격 빈 공간 우클릭 → "New Folder" (구현 시 context menu 추가 확인) → 원격 폴더 생성
7. 원격 텍스트 파일 클릭 → preview pane에 head streaming 표시 (< 1s)
8. 원격 파일에서 Space → lazy download (progress in chip) → Quick Look 뜸 → 같은 파일 Space 재시도 시 즉시 뜸 (cache hit)
9. ssh_config에 `Host ec2-via-ssm` + `ProxyCommand aws ssm start-session …` 블록 추가 → 연결 성공
10. ssh_config에 `Host prod-via-cf` + `ProxyCommand cloudflared access ssh --hostname %h` 블록 추가 → 연결 성공
11. 5분 대기 (커피) → 사이드바 호스트 dot → gray 전환 (idle reap) → 재연결 시 TOFU 없이 바로 붙음
12. 사이드바 호스트 우클릭 → Disconnect → dot 즉시 gray → Connect → 다시 green
13. `ssh-keygen -R prod-api` 후 다른 ssh 서버 키를 prod-api로 속여서 세팅 → 연결 시 Changed-key 빨간 alert → Cancel만 가능 → abort됨
14. (보너스) View > "Clear Remote Cache" 클릭 → `~/Library/Caches/Cairn/remote/` 비워짐
15. 앱 quit (⌘Q) → 모든 세션 graceful close (Activity Monitor에서 cloudflared 프로세스도 같이 종료)

> 체크리스트 중 실패 항목이 있으면 해당 Task로 돌아가 수정 + 해당 커밋 위에 fixup 커밋 쌓음.

- [ ] **Step 4: 태그 + push-ready 상태 확인**

```bash
git log --oneline main..HEAD | head -30
git status
git tag phase-2-ssh-v1
git tag v0.2.0-alpha.1
```

Expected:
- log가 Task 0~26 순서의 커밋 ~30개 근처
- `git status` clean
- 두 태그가 같은 HEAD에 붙음

- [ ] **Step 5: 최종 커밋 (릴리스 노트)**

```bash
git commit --allow-empty -m "chore(release): SSH/SFTP v1 milestone complete · phase-2-ssh-v1

v0.2.0-alpha.1 — first SSH release.

Highlights:
- ssh_config-sourced sidebar 'Remote Hosts' section
- Connect sheet (⇧⌘K) with ssh_config append-on-save
- TOFU + changed-key + passphrase modals (shared ~/.ssh/known_hosts,
  Keychain option for passphrases)
- Pool-per-host with 5min idle timeout + manual Disconnect/Reconnect
- Bidirectional drag-drop + rename/delete/mkdir on remote
- Text head streaming preview + lazy QuickLook cache (500MB LRU) +
  read-only Open With
- Cloudflare Tunnel and AWS SSM hosts work day one via ProxyCommand
- New cairn-ssh Rust crate (russh 0.44, russh-sftp 2.0)
- FileSystemProvider abstraction — local/remote unified via FSPath

Explicit M2 deferrals: dual-pane, transfer queue manager, resume,
edit-in-place, content search, chmod, password/kbd-interactive auth.

Parent spec: docs/superpowers/specs/2026-04-23-cairn-ssh-v1-design.md"
```

---

## Self-review notes (implementer awareness)

이 플랜을 실행할 때 다음 상호 의존성을 숙지:

1. **Task 8 (FFI) → Task 12 (Service) → Task 17 (callback 배선)**: swift-bridge callback 패턴은 3단계에 걸쳐 완성. Task 8에서 스텁, Task 12에서 Swift-side shell, Task 17에서 양방향 wiring.
2. **Task 10 (FolderModel/Tab refactor)**: 이것이 로컬 회귀의 최대 위험 지점. 커밋 전 기존 XCTest 60+ 건 전수 PASS 확인을 Step으로 명시했음.
3. **Task 22 (drag-drop)**: `app` / `provider` 참조를 Coordinator에 주입하는 부분이 `updateBindings` 확장 작업 필요. Task 19에서 이미 provider는 주입되므로 transfers만 추가.
4. **Task 23 (cache)**: `RemoteCacheStore`의 staleness 체크는 SFTP stat의 mtime 해상도가 초 단위라는 점 유의. 동일 초 내 두 번 업데이트되면 stale 감지 실패 가능 — 드물어서 v1엔 수용.
5. **Task 6 (SFTP wrapper)**: `russh-sftp` 2.x API가 마이너 버전 간 바뀔 수 있음. 시작 전 `cargo doc --open -p russh-sftp`로 실제 method signature 확인 후 이 task의 코드 조정.
6. **에러 번역 품질**: 각 task의 에러 경로는 사용자 메시지 품질이 핵심. 릴리스 전 Task 24 completion 후 한 번 더 sweep — 모든 NSAlert / RemoteErrorCard 문구를 소리내어 읽어보며 어색한 것 수정.
